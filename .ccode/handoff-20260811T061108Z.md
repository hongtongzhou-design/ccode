# 接力简报（AI 提炼）：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全

> 本简报由 Ccode 用 AI 从 kimi 会话全文提炼（2026-08-11T06:11:08Z），是结构化摘要而非完整记忆。

## 任务信息

- 来源 Agent：kimi
- 项目目录：/Users/tongzhouhong/Documents/Ccode
- 会话标题：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全
- 会话 ID：session_88fe53d5-b142-4320-aa91-00e0af6adf09

## 当前 git 状态

- 分支：main（领先 0 / 落后 0）
- 改动 8 个文件（+686 -0）：
  - ?? .ccode/handoff-20260811T060736Z.md （+78 -0）
  - ?? .ccode/handoff-20260811T060745Z.md （+86 -0）
  - ?? .ccode/handoff-20260811T060849Z.md （+85 -0）
  - ?? .ccode/handoff-20260811T060850Z.md （+90 -0）
  - ?? .ccode/handoff-20260811T060908Z.md （+79 -0）
  - ?? .ccode/handoff-20260811T060912Z.md （+92 -0）
  - ?? .ccode/handoff-20260811T060944Z.md （+80 -0）
  - ?? .ccode/handoff-20260811T060948Z.md （+96 -0）

## 会话要点（AI 提炼）

• ## 任务目标

  会话中依次有三个目标，均已完成：

  1. 修复侧栏收缩后 hover/选中衬底被裁切的问题（注意：用户澄清过裁的是鼠标悬停显示的衬底，不是图标；底部控制面板和设置衬底显示正常）。
  2. 从第一性原理分析顶部胶囊模块与软件/科研工作的兼容性（纯讨论，无代码改动）。
  3. 「全部执行」——按上一轮盘点出的噪音清单，对工作区页和终端页做注意力降噪清理。

  ## 关键决策与结论

  - **侧栏裁切根因**：`src/App.css` 给 `.ccode-app-nav` 设了 `scrollbar-gutter: stable`。用户系统滚动条为"始终显示"（经典滚动条），侧栏收缩成 56px 后仍强制预留 ~14px 滚动条沟槽，内容盒只剩 ~29px，而收缩态按钮固定 `w-11`（44px），衬底右半截被 `overflow-x-hidden` 裁掉。图标居中故未裁；底部「命令面板/设置」不在滚动容器内故衬底完整。已用真实截图像素测量验证（选中衬底实际 52px，缺口正好 14pt）。
  - **胶囊模块本质**：整个系统唯一的注意力路由口，回答"此刻有没有事在等我拍板"。三条不变量：稀少性（信噪比）、真实性（条目对应可执行动作）、可达性（一键路由到拍板现场）。只有"人类决策是唯一 unblock 条件"的事件够格进入；排序框架为「冲突 > 待确认 > 可合并」，即"不动就烂 > 动了才走 > 可动可不动"。
  - **「已回复」全链路清除的依据**：回合结束 ≠ 有事要做，每回合都会发生 = 纯噪音。此结论与 v3.59 否决步进器绿点同源，据此删除了全部五处残留。真信号不需要已读机制。
  - **降噪口径**：纯状态不用语义色伪装待办；瞬态反馈自动消退；无限 `animate-pulse` 禁止（有界的 `animate-pulse-brief` 除外）；新状态指示进界面前先过"是否阻塞人的决策"闸。

  ## 已完成的改动（文件 + 要点）

  侧栏修复（两处）：

  - `src/App.css:364` — `.ccode-app-nav` 移出 `scrollbar-gutter: stable` 规则（保留 `.ccode-app-main`），附注释说明原因。
  - `src/App.tsx:385` — 收缩态导航按钮 `w-11` 改为 `w-full justify-center`，极端情况（窗口极矮、nav 真出经典滚动条）下衬底自动收窄而不再被裁。

  降噪清理（「全部执行」部分）：

  - `WorkspacesPage.tsx` — 收件箱移除 `done:` 条目与摘要「N 已回复」，排序变为 冲突 > 待确认 > 可合并；「✓ 工作区已创建」横幅 10 秒自动消退（setup 失败不自动收）；项目导航行副行删「N 个活跃任务」，仅有待处理时显示「M 个待处理」；端口「N 个监听中」改为展开后才显示（约 :754）。
  - `TerminalPage.tsx` — 删除标签蓝点与整套 seenDoneRef 已读机制（约 :2879-2908）；「工作中」无限脉冲改为有界 `animate-pulse-brief`（约 :2899）；「端口段已注入」（约 :1518）、「可恢复」（约 :2912）、「上次任务」去 pill 底色降为普通灰字。
  - `ProjectRail.tsx:279-288` — 工作区行 done 蓝点移除，只留 待确认 > 工作中 > 运行中。
  - `ProjectGroup.tsx:925` — 分组头「N 进行中 / N 待评审」绿/主色点降灰，只有「阻塞」保留语义色。
  - `notify.ts:42-49` + `App.tsx` — OS 通知只发「待确认」，移除 `NotifyKind`/done 分支及「已回复直达评审」链路（App.tsx 里清的是死代码）。
  - `run-overview.ts` — done 不占排序档、不进摘要，`itemRank`/`buildRunOverview` 签名简化，两个测试文件已同步。
  - `ArtifactChecklist.tsx:190` — 删除「刚更新」标记。
  - 文档：`AGENTS.md` 收件箱条改写并新增「注意力信噪比总规则（v3.60）」；`docs/user-guide.md` 七处描述同步（含修正步进器角标"绿点=已完成"的过期描述）。

  ## 当前状态与未完成事项

  - 构建通过，测试全绿（侧栏修复后 57 过；降噪后 56 过，因 done 相关测试被删改）。改动经 HMR 已热更到用户正在运行的 Ccode Dev 窗口。
  - 「全部执行」清单内事项无遗留。
  - 未做但已识别的遗留点：「可合并」信息在终端 pill（`TerminalPage.tsx` 约 :2946）、ProjectRail 状态点、步进器角标三处重复镜像——当时定性为"不制造新噪音但有口径漂移风险"，未合并。

  ## 下一步建议

  - 若用户要继续扩胶囊来源，既定优先级为：产物待核验 > 基准前进后的冲突重同步 > profile 失效 > 接力待发送；前两项可直接复用 `artifacts.yaml` mtime 与 merge-base 比对的现成数据，经 `terminalRunInputs`/`inboxItems` 镜像消费模式延伸，不需要新轮询。
  - 新来源接入约束：`InboxItem` 为可序列化条目 + `runInboxAction` 统一派发（`src/store.ts:114`），新来源 = 写一个检测器 + 必要时加一类 action，不动消费方；写入必须走 WorkspacesPage 签名去抖镜像这单一写入方。
  - 继续拒绝"进度展示、完成计数、运行状态"类进胶囊的需求。
  - 可请用户在 dev 窗口里目视确认侧栏收缩态衬底与终端页降噪效果。

  ## 环境与约束

  - 项目为 Ccode（React/TS + vite），含 `AGENTS.md` 约定文件与 `docs/user-guide.md`，改动须同步这两处文档；版本记号形如 v3.59/v3.60。
  - macOS，用户系统滚动条设置为"始终显示"（经典滚动条），任何 `scrollbar-gutter` / overflow 相关改动都必须在此设置下验证；可用 Safari（同为 WebKit）对比渲染。
  - 用户本机有一个 11:51 启动的 Ccode Dev 实例占着 vite 端口，不要动它；重复拉起实例会因端口占用失败。
  - 改动依赖 HMR 即时生效，验证方式为构建 + 测试套件（当前 56 个测试）。

## 接力说明

- 本简报由 Ccode 从 kimi 会话生成，非完整记忆。
- 源会话的完整上下文仍保留在原 Agent 中；请结合项目文件与 git 历史补全背景。
- 不要假设你知道简报之外的对话细节；不确定时先读代码与相关文件。
