# 接力简报（AI 提炼）：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全

> 本简报由 Ccode 用 AI 从 kimi 会话全文提炼（2026-08-11T06:07:45Z），是结构化摘要而非完整记忆。

## 任务信息

- 来源 Agent：kimi
- 项目目录：/Users/tongzhouhong/Documents/Ccode
- 会话标题：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全
- 会话 ID：session_88fe53d5-b142-4320-aa91-00e0af6adf09

## 当前 git 状态

- 分支：main（领先 0 / 落后 0）
- 改动 1 个文件（+78 -0）：
  - ?? .ccode/handoff-20260811T060736Z.md （+78 -0）

## 会话要点（AI 提炼）

• ## 任务目标

  在 Ccode 项目（vite + React/TS 桌面应用前端）中持续降低界面注意力噪音，核心主线：

  1. （已完成）修复侧栏收缩后 hover/选中衬底被裁切的问题。
  2. （已完成分析）从第一性原理论证顶部胶囊模块（收件箱/注意力路由口）的兼容边界。
  3. （已完成）盘点并清除工作区页与终端页的注意力噪音——用户指令「全部执行」，已全部落地。

  ## 关键决策与结论

  - **胶囊模块三不变量**（后续一切增删的裁决标准）：
    - 稀少性：胶囊价值 = 信噪比，不阻塞决策的信息不进；
    - 真实性：条目必须对应当刻可执行的真实动作；
    - 可达性：每条一键路由到拍板现场，胶囊自身不变成操作界面。
  - **唯一准入闸**：「是否存在一个只有人能做、且不做就阻塞的动作」。纯状态（运行数、token 用量、端口列表、文件新旧）一律不用语义色、不进胶囊。
  - **排序框架**：冲突 > 待确认 > 可合并（原「已回复」档已整体废除，依据：回合结束 ≠ 有事要做，每轮都发生 = 纯噪音）。
  - **已确立的降噪口径**（写入 AGENTS.md v3.60）：已回复全链路无标记；纯状态不用语义色；瞬态横幅自动消退；禁止无限 `animate-pulse`（用有界 `animate-pulse-brief`）；计数只出现在胶囊/strip。
  - **历史上已被否决的方案**（不要再提）：「已回复」绿点、侧栏数字徽标、状态点撞语义、进度展示/完成计数/运行状态类指示。
  - **胶囊未来扩展的候选与优先级**（仅结论，未实现）：产物待核验 > 基准已前进的冲突重同步 > profile/密钥失效 > 接力简报待发送。检测数据可复用 `artifacts.yaml` mtime、merge-base 比对，不需新轮询。

  ## 已完成的改动（文件 + 要点）

  侧栏衬底裁切修复：

  - `src/App.css:364` — `.ccode-app-nav` 移出 `scrollbar-gutter: stable` 规则（保留 `.ccode-app-main`），附注释。根因：经典滚动条下 stable 沟槽占 ~14px，44px 按钮衬底被 `overflow-x-hidden` 裁掉。
  - `src/App.tsx:385` — 收缩态导航按钮 `w-11` 改为 `w-full justify-center`。

  「已回复」全链路清除 + 降噪：

  - `src/pages/WorkspacesPage.tsx`（路径以仓库实际为准，记录中写作 `WorkspacesPage.tsx`）— 收件箱 `done:` 条目与「N 已回复」摘要移除；「✓ 工作区已创建」横幅 10 秒自动消退（setup 失败除外）；项目导航副行删「N 个活跃任务」，仅在有待处理时显示「M 个待处理」（原 :1638 附近）；端口「N 个监听中」改为展开后才显示（原 :754 附近）；「✓ 工作区已创建」横幅原 :1682 附近。
  - `TerminalPage.tsx` — 标签「已回复」蓝点与整套 `seenDoneRef` 已读机制删除（原 :2879-2908）；「工作中」无限脉冲改 `animate-pulse-brief`（原 :2899）；「端口段已注入」（原 :1518）、「可恢复」徽标（原 :2912）、「上次任务」去 pill 底色降灰。
  - `ProjectRail.tsx`（原 :279-288）— 工作区行 done 蓝点移除，只留 待确认 > 工作中 > 运行中。
  - `ProjectGroup.tsx:925` — 分组头「N 进行中 / N 待评审」绿/主色点降灰，仅「阻塞」保留语义色。
  - `notify.ts`（原 :42-49）— OS 通知只发「待确认」，「已回复」通知及 `NotifyKind`/done 分支移除。
  - `App.tsx` — 通知动作里 done 分支死代码清理。
  - `run-overview.ts` — done 不占排序档、不进摘要；`itemRank`/`buildRunOverview` 签名简化。
  - `ArtifactChecklist.tsx:190` — 「刚更新」标记删除。
  - 两个对应测试文件同步更新。
  - `AGENTS.md` — 收件箱条目改写，新增「注意力信噪比总规则（v3.60）」。
  - `docs/user-guide.md` — 七处描述同步（含修正步进器角标「绿点=已完成」的过期描述）。

  注：`runInboxAction` 在 `src/store.ts:114`，action 现有 review/tab/session 三类；收件箱单一写入方是 WorkspacesPage 的签名去抖镜像（接新来源必须走聚合并去重，防计数漂移）。

  ## 当前状态与未完成事项

  - 构建通过，56 个测试全绿；改动已通过 HMR 热更到用户正在运行的 Ccode Dev 窗口，用户可直接查看效果。
  - 未实现的提案（仅有分析结论，用户尚未要求动手）：胶囊四类新来源——产物待核验（`artifacts.yaml` 登记产物已产出未验收）、产物逾期未产出（warn）、基准前进的冲突重同步、profile/密钥失效、接力简报待发送。

  ## 下一步建议

  - 若用户要求落地胶囊扩展，按已排优先级做：产物待核验 > 冲突重同步 > profile 失效 > 接力待发送；每类新来源 = 一个检测器 + 必要时新增一类 inbox action，不改消费方。
  - 请用户在实际窗口中目测确认降噪效果（蓝点、脉冲、横幅消退等），有反弹再局部回调。
  - 任何新状态指示进界面前，先用「是否阻塞人的决策」过闸，并同步 AGENTS.md v3.60 规则。

  ## 环境与约束

  - 项目为 vite + React + TypeScript；改动经 HMR 即时生效；测试套件 56 个用例（构建与测试命令沿用仓库既有脚本）。
  - 用户本机有一个 11:51 自行启动的 Ccode Dev 实例在运行，**不要动它**；端口被占用说明 dev server 已在跑，勿重复拉起。
  - 仓库有 `AGENTS.md` 约定文件，修改界面行为/状态指示口径时必须同步维护它及 `docs/user-guide.md`。
  - 系统滚动条设置为「始终显示」（经典滚动条），涉及滚动容器的样式改动需在此设定下验证。
  - 仓库根路径在会话记录中未出现，续作时需先定位项目目录再操作。

## 接力说明

- 本简报由 Ccode 从 kimi 会话生成，非完整记忆。
- 源会话的完整上下文仍保留在原 Agent 中；请结合项目文件与 git 历史补全背景。
- 不要假设你知道简报之外的对话细节；不确定时先读代码与相关文件。
