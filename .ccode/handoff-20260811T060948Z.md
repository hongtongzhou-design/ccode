# 接力简报（AI 提炼）：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全

> 本简报由 Ccode 用 AI 从 kimi 会话全文提炼（2026-08-11T06:09:48Z），是结构化摘要而非完整记忆。

## 任务信息

- 来源 Agent：kimi
- 项目目录：/Users/tongzhouhong/Documents/Ccode
- 会话标题：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全
- 会话 ID：session_88fe53d5-b142-4320-aa91-00e0af6adf09

## 当前 git 状态

- 分支：main（领先 0 / 落后 0）
- 改动 7 个文件（+590 -0）：
  - ?? .ccode/handoff-20260811T060736Z.md （+78 -0）
  - ?? .ccode/handoff-20260811T060745Z.md （+86 -0）
  - ?? .ccode/handoff-20260811T060849Z.md （+85 -0）
  - ?? .ccode/handoff-20260811T060850Z.md （+90 -0）
  - ?? .ccode/handoff-20260811T060908Z.md （+79 -0）
  - ?? .ccode/handoff-20260811T060912Z.md （+92 -0）
  - ?? .ccode/handoff-20260811T060944Z.md （+80 -0）

## 会话要点（AI 提炼）

• ## 任务目标

  围绕 Ccode 桌面端的「注意力信噪比」做三件事：

  1. 修复侧栏收缩后 hover/选中衬底（不是图标）右半被裁切的问题。
  2. 从第一性原理论证顶部胶囊模块的定位与兼容边界。
  3. 盘点工作区页与终端页的注意力噪音，并按用户「全部执行」的指令完成全链路降噪。

  ## 关键决策与结论

  - **侧栏裁切根因**：`src/App.css` 给 `.ccode-app-nav` 设了 `scrollbar-gutter: stable`；用户系统滚动条为"始终显示"，收缩成 56px 后仍强制预留 ~14px 沟槽，内容盒只剩 ~29px，固定 44px（`w-11`）按钮的衬底被 `overflow-x-hidden` 裁掉。图标居中故不裁，底部控制面板不在滚动容器内故完整。
  - **胶囊模块本质**：系统唯一的注意力路由口，回答"此刻有没有事在等我拍板"。三条不变量：稀少性（信噪比，不阻塞决策的信息不进）、真实性（每条对应真实可执行动作）、可达性（一键路由到拍板现场，胶囊自身不变成操作界面）。合格标准 = "人类决策是唯一 unblock 条件"。
  - **排序框架**：冲突 > 待确认 > 可合并 > 已回复，即"不动就烂 > 动了才走 > 可动可不动 > 已经动了"。
  - **「已回复」判定为纯噪音**：回合结束 ≠ 有事要做、每回合都发生，全链路五处残留（终端标签蓝点、ProjectRail 蓝点、收件箱条目、run-overview done 计数、OS 通知）全部清除，连带删除 seenDoneRef 已读机制（真信号不需要已读）。
  - **其余降噪口径**：无限 `animate-pulse` 禁留（改有界 `animate-pulse-brief`）；纯状态不用语义色（分组头只有「阻塞」保留语义色）；瞬态横幅自动消退；计数只在胶囊/strip，导航行副行删活跃任务计数；「刚更新」「可恢复」「端口段已注入」等不阻塞决策的标记删除或降灰。
  - 该口径已固化为 `AGENTS.md`「注意力信噪比总规则（v3.60）」：新状态指示进界面前先过「是否阻塞人的决策」闸。

  ## 已完成的改动（文件 + 要点）

  **侧栏修复（先完成，测试 57 过）**：

  - `src/App.css:364` — `.ccode-app-nav` 移出 `scrollbar-gutter: stable` 规则（`.ccode-app-main` 保留），加注释说明原因。
  - `src/App.tsx:385` — 收缩态导航按钮 `w-11` 改为 `w-full justify-center`（正常等宽，极端出现经典滚动条时衬底自动收窄）。

  **「已回复」全链路清除**：

  - `src/WorkspacesPage.tsx` — 收件箱 `done:` 条目与摘要「N 已回复」移除，排序变为 冲突 > 待确认 > 可合并；「✓ 工作区已创建」横幅 10 秒自动消退（setup 失败不自动收）；项目导航行副行删「N 个活跃任务」，仅有待处理时显示「M 个待处理」；端口「N 个监听中」改为展开后才显示。
  - `src/TerminalPage.tsx` — 标签蓝点与整套 seenDoneRef 已读机制删除；「工作中」无限脉冲 → `animate-pulse-brief`；「端口段已注入」「可恢复」「上次任务」去 pill 底色与蓝色，降为普通灰字。
  - `src/ProjectRail.tsx` — 工作区行 done 蓝点移除，只留 待确认 > 工作中 > 运行中。
  - `src/notify.ts` — 只发「待确认」，`NotifyKind`/done 分支移除。
  - `src/App.tsx` — 通知动作里 done 分支死代码、「已回复直达评审」链路清理。
  - `src/run-overview.ts` — done 不占排序档、不进摘要，`itemRank`/`buildRunOverview` 签名简化。
  - 两个对应测试文件同步更新。

  **其余降噪**：

  - `src/ProjectGroup.tsx:925` — 分组头「N 进行中 / N 待评审」绿/主色点降灰，仅「N 阻塞」保留语义色。
  - `src/ArtifactChecklist.tsx:190` — 「刚更新」标记删除。

  **文档同步**：

  - `AGENTS.md` — 收件箱条改写；新增「注意力信噪比总规则（v3.60）」。
  - `docs/user-guide.md` — 七处描述同步（含顺带修正步进器角标"绿点=已完成"的过期描述）。

  ## 当前状态与未完成事项

  - 全部执行完毕：构建通过，56 个测试全绿（原 57 个，done 相关测试随功能删除）。HMR 已热更到用户开着的 Ccode Dev 窗口。
  - 全库残留引用已检查，无遗漏。
  - 未完成：胶囊模块的**扩展落地**仅停留在讨论建议层面，未写任何代码。候选按优先级：产物待核验（基于 `artifacts.yaml` mtime）> 基准前进导致的冲突重同步（merge-base 比对）> profile 三层验证失效/密钥过期 > 接力待发送。接入方式约定为：新来源 = 写一个检测器 + 必要时给 `runInboxAction`（`src/store.ts:114`）加一类 action，消费方不动；写入只能走 WorkspacesPage 签名去抖镜像这一单一写入方。
  - 视觉上尚未经用户人工确认（依赖用户看开发窗口）。

  ## 下一步建议

  1. 让用户在 Ccode Dev 窗口实际查看侧栏收缩态衬底、终端标签、工作区页的新状态，确认无误。
  2. 若继续做胶囊扩展，从「产物待核验」起步：数据现成（`artifacts.yaml` 登记产物的 mtime），不需要新轮询，从 `terminalRunInputs`/`inboxItems` 镜像消费模式延伸即可。
  3. 此后任何"进度展示、完成计数、运行状态"类需求应继续拒绝——胶囊的价值在于大多数时间是空的；新指示一律先过 v3.60 的「是否阻塞人的决策」闸。

  ## 环境与约束

  - macOS，用户系统滚动条设置为"始终显示"（经典滚动条），这是侧栏 bug 的触发条件，验证时需注意覆盖层滚动条系统无法复现。
  - 项目为 React/TypeScript + Vite（HMR 生效中），样式混用 Tailwind 类与 `src/App.css` 自定义样式。
  - 用户本机有一个自行启动的 Ccode Dev 实例（占用 vite 默认端口），不要动它；重复拉起实例会因端口占用退出。
  - 调试产物（Safari 测试页、/tmp 截图）已清理。
  - 仓库约定集中在 `AGENTS.md`：改界面行为须同步该文件及 `docs/user-guide.md`；版本化规则条目（当前至 v3.60）。
  - 验证标准：构建通过 + 全部测试绿（当前基线 56 个）。

## 接力说明

- 本简报由 Ccode 从 kimi 会话生成，非完整记忆。
- 源会话的完整上下文仍保留在原 Agent 中；请结合项目文件与 git 历史补全背景。
- 不要假设你知道简报之外的对话细节；不确定时先读代码与相关文件。
