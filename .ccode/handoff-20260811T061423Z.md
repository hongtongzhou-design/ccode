# 接力简报（AI 提炼）：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全

> 本简报由 Ccode 用 AI 从 kimi 会话全文提炼（2026-08-11T06:14:23Z），是结构化摘要而非完整记忆。

## 任务信息

- 来源 Agent：kimi
- 项目目录：/Users/tongzhouhong/Documents/Ccode
- 会话标题：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全
- 会话 ID：session_88fe53d5-b142-4320-aa91-00e0af6adf09

## 当前 git 状态

- 分支：main（领先 0 / 落后 0）
- 改动 13 个文件（+1081 -0）：
  - ?? .ccode/handoff-20260811T060736Z.md （+78 -0）
  - ?? .ccode/handoff-20260811T060745Z.md （+86 -0）
  - ?? .ccode/handoff-20260811T060849Z.md （+85 -0）
  - ?? .ccode/handoff-20260811T060850Z.md （+90 -0）
  - ?? .ccode/handoff-20260811T060908Z.md （+79 -0）
  - ?? .ccode/handoff-20260811T060912Z.md （+92 -0）
  - ?? .ccode/handoff-20260811T060944Z.md （+80 -0）
  - ?? .ccode/handoff-20260811T060948Z.md （+96 -0）
  - ?? .ccode/handoff-20260811T061108Z.md （+84 -0）
  - ?? .ccode/handoff-20260811T061121Z.md （+85 -0）
  - ?? .ccode/handoff-20260811T061356Z.md （+66 -0）
  - ?? .ccode/handoff-20260811T061359Z.md （+66 -0）
  - ?? .ccode/handoff-20260811T061420Z.md （+94 -0）

## 会话要点（AI 提炼）

• ## 任务目标

  已完成两项（见下），当前接力的任务是执行四项胶囊（收件箱）扩展优化，优先级为：**产物待核验 > 基准前进的冲突重同步 > profile 失效 > 接力待发送**；同时改造接力简报生成流程：现在点「◈ 提炼接力」会一直停在等待页、重新点开就重新生成，需改为**后台生成、生成完成后展示**，重复打开直接显示已有结果而不是重新生成。

  ## 关键决策与结论

  - **胶囊模块本质**：整个系统唯一的注意力路由口，价值 = 信噪比。三条不变量：稀少性（不阻塞决策的信息进来一条可信度贬一分）、真实性（条目必须对应当刻可执行的真实动作）、可达性（一键路由到拍板现场，胶囊自身不变成操作界面）。准入标准只有一条：**人类决策是否是唯一 unblock 条件**。
  - **排序框架**：冲突 > 待确认 > 可合并 > 已回复，即「不动就烂 > 动了才走 > 可动可不动 > 已经动了」。新来源答不上自己属哪一档就别进。
  - **架构结论**：`InboxItem` 可序列化 + `runInboxAction` 统一派发（`src/store.ts:114`，现有 review/tab/session 三类 action）；接新来源 = 写一个检测器 + 必要时加一类 action，不动消费方。单一写入方约束（WorkspacesPage 签名去抖镜像），新来源只能走「聚合并去重」，无需新轮询，从 `terminalRunInputs`/`inboxItems` 镜像消费模式延伸即可。
  - **明确拒绝项**：运行中数量、token 用量、端口监听列表、进度展示、完成计数等纯状态一律不进胶囊——胶囊的价值在于它大多数时间是空的。
  - **侧栏裁切根因**（已修复）：`scrollbar-gutter: stable` 叠加系统「始终显示」经典滚动条，56px 收缩侧栏被强留 ~14px 沟槽，内容盒只剩 ~29px < 44px 按钮，hover 衬底右半截被 `overflow-x-hidden` 裁掉（截图像素测量证实缺口正好 14px）。
  - **降噪总规则**（已写入 AGENTS.md v3.60）：已回复全链路无标记、纯状态不用语义色、瞬态横幅自动消退、禁无限 animate-pulse、新状态指示进界面前先过「是否阻塞人的决策」闸。

  ## 已完成的改动（文件 + 要点）

  **1. 侧栏收缩态 hover 衬底裁切修复**（测试 57 过 0 失败）

  - `src/App.css:364` — `.ccode-app-nav` 移出 `scrollbar-gutter: stable` 规则（保留 `.ccode-app-main`），附注释说明原因。
  - `src/App.tsx:385` — 收缩态导航按钮 `w-11` 改为 `w-full justify-center`，极端情况下衬底自动收窄而不再被裁。

  **2. 「已回复」全链路清除 + 工作区/终端降噪**（构建通过，56 测试全绿）

  - `src/WorkspacesPage.tsx` — 收件箱 done 条目与摘要「N 已回复」移除，排序变为 冲突 > 待确认 > 可合并；「✓ 工作区已创建」横幅 10 秒自动消退（setup 失败不自动收）；项目导航副行删「N 个活跃任务」，仅有待处理时显示「M 个待处理」；端口「N 个监听中」改为展开后才显示。
  - `src/TerminalPage.tsx` — 标签「已回复」蓝点与整套 seenDoneRef 已读机制删除；「工作中」无限脉冲改为有界 `animate-pulse-brief`；「端口段已注入」「可恢复」「上次任务」去 pill 底色与蓝色，降为普通灰字。
  - `src/ProjectRail.tsx` — 工作区行 done 蓝点移除，只留 待确认 > 工作中 > 运行中。
  - `src/ProjectGroup.tsx` — 分组头「N 进行中 / N 待评审」的绿/主色点降灰，仅「阻塞」保留语义色。
  - `src/notify.ts` — OS 通知只发「待确认」，移除 `NotifyKind` done 分支与「已回复，等你查看」通知。
  - `src/App.tsx` — 清理通知动作里的 done 分支死代码（含「已回复直达评审」链路）。
  - `src/run-overview.ts` — done 不占排序档、不进摘要，`itemRank`/`buildRunOverview` 签名简化；对应两个测试文件已同步。
  - `src/ArtifactChecklist.tsx` — 「刚更新」标记删除。
  - 文档：`AGENTS.md` 收件箱条改写并新增「注意力信噪比总规则（v3.60）」；`docs/user-guide.md` 七处同步（含顺带修正步进器角标「绿点=已完成」的过期描述）。

  ## 当前状态与未完成事项

  - 上述两项改动已全部完成并验证（构建 + 测试全绿，HMR 已生效）。
  - 四项胶囊扩展均未开工：
    - 产物待核验（最高优先级）
    - 基准前进后的冲突重同步
    - profile 失效（三层验证失败、密钥过期）
    - 接力待发送（含生成流程后台化改造）

  ## 下一步建议

  - 按既定优先级逐项实施，每项遵循同一模式：**检测器 → 聚合并去重 → 汇入 WorkspacesPage 镜像**，必要时在 `runInboxAction` 增加 action 类型。
  - 产物待核验：以 `artifacts.yaml` 登记的预期产物 mtime 检测「已产出未验收」（图渲染完、data-clean 跑完、quarto render 出 PDF）；可另加「产物逾期未产出」warn（可能 agent 卡住）。
  - 冲突重同步：用 merge-base 比对检测「基准已前进，冲突两侧需重新同步」。
  - 接力待发送：先定位「◈ 提炼接力」的生成入口与等待页逻辑，改为异步后台生成 + 结果缓存；生成完成经胶囊提示「接力待发送」，用户点开后直接展示结果，不重新生成。
  - 全程遵守 v3.60 信噪比规则：任何新指示先自证「阻塞人的决策」。

  ## 环境与约束

  - 项目：Ccode，React + TypeScript + Vite + vitest；验收标准为构建通过 + 测试全绿（当前 56 个测试）。
  - 用户系统（macOS）滚动条为「始终显示」的经典滚动条，涉及滚动容器的布局改动需把沟槽占位纳入考量。
  - 用户本机有一个 11:51 自行启动的 Ccode Dev 实例在运行（占用 vite 默认端口），不要动它；改动经 HMR 即时生效，如需自起 dev server 需换端口。
  - `AGENTS.md` 是项目约定源（当前 v3.60），凡改动交互/状态指示约定，须同步 `AGENTS.md` 与 `docs/user-guide.md`。
  - git 提交等变更操作需先征得用户确认。

## 接力说明

- 本简报由 Ccode 从 kimi 会话生成，非完整记忆。
- 源会话的完整上下文仍保留在原 Agent 中；请结合项目文件与 git 历史补全背景。
- 不要假设你知道简报之外的对话细节；不确定时先读代码与相关文件。
