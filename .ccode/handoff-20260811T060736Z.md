# 接力简报（AI 提炼）：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全

> 本简报由 Ccode 用 AI 从 kimi 会话全文提炼（2026-08-11T06:07:36Z），是结构化摘要而非完整记忆。

## 任务信息

- 来源 Agent：kimi
- 项目目录：/Users/tongzhouhong/Documents/Ccode
- 会话标题：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全
- 会话 ID：session_88fe53d5-b142-4320-aa91-00e0af6adf09

## 当前 git 状态

- 分支：main（领先 0 / 落后 0）
- 工作区干净，无未提交改动

## 会话要点（AI 提炼）

• ## 任务目标

  1. 修复侧栏收缩态下 hover/选中衬底被裁切的问题（图标本身未被裁，底部控制面板/设置衬底正常）。
  2. 从第一性原理分析顶部胶囊模块与软件/科研工作的兼容性（纯分析，未要求落地）。
  3. 「全部执行」工作区页与终端页的降噪清单：清除「已回复」全链路标记及各类纯状态噪音指示。

  ## 关键决策与结论

  - **裁切根因**：`src/App.css` 给 `.ccode-app-nav` 设了 `scrollbar-gutter: stable`；系统滚动条"始终显示"时，56px 收缩侧栏被强制预留 ~14px 滚动条沟槽，内容盒只剩 ~29px，44px 固定宽按钮的衬底右半被 `overflow-x-hidden` 裁掉。已用真实截图像素测量验证（衬底实测 52px，缺口正好 14pt）。
  - **胶囊模块三不变量**：稀少性（信噪比）、真实性（条目对应此刻可执行动作）、可达性（一键路由到拍板现场，胶囊自身不变操作界面）。新来源准入标准只有一条："人类决策是否是唯一 unblock 条件"。
  - **排序口径**：冲突 > 待确认 > 可合并（「已回复」档已整体删除），本质是"不动就烂 > 动了才走 > 可动可不动"。
  - **注意力信噪比总规则（v3.60，已写入 AGENTS.md）**：已回复全链路无标记、纯状态不用语义色、瞬态横幅自动消退、禁止无限 pulse；任何新状态指示进界面前先过「是否阻塞人的决策」闸。
  - 「已回复」被否的论据：回合结束 ≠ 有事要做，每回合都发生 = 纯噪音；真信号不需要已读机制。
  - 胶囊扩展候选优先级（仅分析结论，未实施）：产物待核验 > 基准前进的冲突重同步 > profile 失效 > 接力待发送；进度展示/完成计数/运行状态类继续拒绝。

  ## 已完成的改动（文件 + 要点）

  **任务 1（侧栏衬底裁切）**：
  - `src/App.css:364` — `.ccode-app-nav` 移出 `scrollbar-gutter: stable` 规则（`.ccode-app-main` 保留），附注释说明原因。
  - `src/App.tsx:385` — 收缩态导航按钮 `w-11` 改为 `w-full justify-center`，极端情况下衬底自动收窄不再被裁。

  **任务 3（降噪，全部完成）**：
  - `src/WorkspacesPage.tsx` — 收件箱移除 `done:` 条目与摘要「N 已回复」；「✓ 工作区已创建」横幅 10 秒自动消退（setup 失败不自动收）；项目导航行副行删「N 个活跃任务」，仅在有待处理时显示「M 个待处理」；端口「N 个监听中」改为展开后才显示。
  - `src/TerminalPage.tsx` — 删除标签蓝点及整套 seenDoneRef 已读机制；「工作中」无限 `animate-pulse` 改有界 `animate-pulse-brief`；「端口段已注入」「可恢复」「上次任务」去 pill 底色降为普通灰字。
  - `src/ProjectRail.tsx` — 工作区行 done 蓝点移除，只留 待确认 > 工作中 > 运行中。
  - `src/notify.ts` — 只发「待确认」通知，`NotifyKind`/done 分支及「已回复直达评审」链路移除。
  - `src/App.tsx` — 清理通知动作里的 done 死代码分支。
  - `src/run-overview.ts` — done 不占排序档、不进摘要；`itemRank`/`buildRunOverview` 签名简化。
  - `src/ProjectGroup.tsx` — 分组头「N 进行中 / N 待评审」绿/主色点降灰，仅「阻塞」保留语义色。
  - `src/ArtifactChecklist.tsx` — 删除「刚更新」标记。
  - 两个测试文件同步更新（对应 `run-overview` 及收件箱逻辑变更）。
  - `AGENTS.md` — 收件箱条改写，新增「注意力信噪比总规则（v3.60）」。
  - `docs/user-guide.md` — 七处描述同步，含顺带修正步进器角标"绿点=已完成"的过期描述。

  ## 当前状态与未完成事项

  - 构建通过，56 个测试全绿；改动已通过 HMR 热更到用户开着的 Ccode Dev 窗口。
  - 未实施（用户未要求）：胶囊模块扩展候选（产物待核验、基准前进冲突重同步、profile 失效、接力待发送）——仅停留在分析层面。
  - 「已回复」相关残留已全库 grep 清查，无遗留引用。

  ## 下一步建议

  - 在运行中的 Ccode Dev 窗口目视确认：侧栏收缩态 hover 衬底完整、终端标签无蓝点、横幅 10 秒自动消退。
  - 若用户提出扩展胶囊，按既定优先级从「产物待核验」做起：检测数据可复用 `artifacts.yaml` mtime 与 merge-base 比对，无需新轮询，走 `InboxItem` + `runInboxAction`（`src/store.ts:114` 附近）聚合并去重的既有链路，新来源只能由 WorkspacesPage 签名去抖镜像单一写入。
  - 任何新增状态指示需求，先按 v3.60 闸门（是否阻塞人的决策）过滤再动手。

  ## 环境与约束

  - macOS，系统滚动条为"始终显示"（经典滚动条），这是任务 1 根因的触发条件。
  - Vite dev server 带 HMR；用户本机有一个自行启动的 Ccode Dev 实例占用默认端口，不要动它；重复拉起实例会因端口占用退出。
  - 测试套件 56~57 个用例（任务 1 后为 57，任务 3 后为 56，因签名简化合并了用例）。
  - 调试产生的 Safari 测试页与 /tmp 截图已清理。
  - 项目约定以 `AGENTS.md` 为准（含 v3.60 注意力规则），改界面行为需同步 `AGENTS.md` 与 `docs/user-guide.md`。
  - 上文行号为修改时快照，后续可能漂移，引用前以实际文件为准。

## 接力说明

- 本简报由 Ccode 从 kimi 会话生成，非完整记忆。
- 源会话的完整上下文仍保留在原 Agent 中；请结合项目文件与 git 历史补全背景。
- 不要假设你知道简报之外的对话细节；不确定时先读代码与相关文件。
