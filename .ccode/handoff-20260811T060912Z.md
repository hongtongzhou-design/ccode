# 接力简报（AI 提炼）：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全

> 本简报由 Ccode 用 AI 从 kimi 会话全文提炼（2026-08-11T06:09:12Z），是结构化摘要而非完整记忆。

## 任务信息

- 来源 Agent：kimi
- 项目目录：/Users/tongzhouhong/Documents/Ccode
- 会话标题：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全
- 会话 ID：session_88fe53d5-b142-4320-aa91-00e0af6adf09

## 当前 git 状态

- 分支：main（领先 0 / 落后 0）
- 改动 5 个文件（+418 -0）：
  - ?? .ccode/handoff-20260811T060736Z.md （+78 -0）
  - ?? .ccode/handoff-20260811T060745Z.md （+86 -0）
  - ?? .ccode/handoff-20260811T060849Z.md （+85 -0）
  - ?? .ccode/handoff-20260811T060850Z.md （+90 -0）
  - ?? .ccode/handoff-20260811T060908Z.md （+79 -0）

## 会话要点（AI 提炼）

• ## 任务目标

  本会话含三项任务，均围绕同一 Ccode 桌面应用（React + Vite）：

  1. **侧栏收缩态 hover 衬底被裁切**（已完成）：收缩后图标本身的 hover/选中衬底右半截缺失，底部控制面板/设置正常。
  2. **顶部胶囊模块第一性原理分析**（已完成，纯分析）：分析胶囊与软件架构、科研流水线的兼容性，产出准入标准与扩展候选。
  3. **工作区/终端注意力噪音「全部执行」清理**（已完成）：按"是否阻塞人的决策"标准清除噪音元素。

  遗留的潜在续作：胶囊/收件箱新来源扩展（仅到建议阶段，未实施）。

  ## 关键决策与结论

  - **侧栏根因**：`.ccode-app-nav` 上的 `scrollbar-gutter: stable` 在经典滚动条（macOS"始终显示"）下强制预留 ~14px 沟槽；收缩态侧栏 56px，内容盒只剩 ~29px，小于按钮固定 `w-11`（44px），衬底被 `overflow-x-hidden` 裁掉。图标居中故未裁，底部面板不在滚动容器内故完整。真实截图像素测量验证缺口恰为 14pt。
  - **胶囊本质**：系统唯一注意力路由口，价值 = 信噪比。三不变量：**稀少性**（不阻塞决策的信息进一条，可信度贬一分）、**真实性**（条目对应真实可执行动作）、**可达性**（一键路由到拍板现场，自身不变成操作界面）。准入标准："人类决策是唯一 unblock 条件"。
  - **排序框架**：冲突 > 待确认 > 可合并（口诀：不动就烂 > 动了才走 > 可动可不动）。"已回复"被判定为纯噪音（回合结束 ≠ 有事要做，每轮都发生），全链路清除。
  - **v3.60 注意力信噪比总规则**（已写入 AGENTS.md）：已回复全链路无标记；纯状态不用语义色；瞬态横幅自动消退；禁无限 `animate-pulse`；新状态指示进界面前先过"是否阻塞人的决策"闸。
  - 明确**不够格进胶囊**的：运行中数量、token 用量、端口监听列表、进度展示、完成计数、`notes/inbox.md` 待整理。
  - 「可合并」在 pill / ProjectRail 状态点 / 步进器角标三处与收件箱同源重复，保留但存在口径漂移风险。

  ## 已完成的改动（文件 + 要点）

  **侧栏修复**：
  - `src/App.css:364` — `.ccode-app-nav` 移出 `scrollbar-gutter: stable` 规则（保留 `.ccode-app-main`），附注释说明原因
  - `src/App.tsx:385` — 收缩态导航按钮 `w-11` → `w-full justify-center`（正常等宽，极端情况衬底自动收窄不再被裁）

  **「已回复」全链路清除**：
  - `WorkspacesPage.tsx` — 收件箱 `done:` 条目与摘要「N 已回复」移除，排序变为 冲突 > 待确认 > 可合并
  - `TerminalPage.tsx:2879-2908` — 终端标签蓝点与整套 `seenDoneRef` 已读机制删除
  - `ProjectRail.tsx:279-288` — 工作区行 done 蓝点移除，只留 待确认 > 工作中 > 运行中
  - `notify.ts:42-49` — 只发「待确认」；`NotifyKind`/done 分支及「已回复直达评审」通知链路移除
  - `App.tsx` — 通知动作里 done 分支死代码删除
  - `run-overview.ts` — done 不占排序档、不进摘要；`itemRank`/`buildRunOverview` 签名简化；两个测试文件同步

  **其余降噪**：
  - `TerminalPage.tsx:2899` — 标签「工作中」无限脉冲 → 有界 `animate-pulse-brief`
  - `WorkspacesPage.tsx:1682` — 「✓ 工作区已创建」横幅 10 秒自动消退（setup 失败不自动收）
  - `TerminalPage.tsx:1518、2912` — 「端口段已注入」「可恢复」「上次任务」去 pill 底色/蓝色，降为普通灰字
  - `ProjectGroup.tsx:925` — 分组头「N 进行中 / N 待评审」绿/主色点降灰，仅「阻塞」保留语义色
  - `WorkspacesPage.tsx:1638` — 项目导航行副行删「N 个活跃任务」，仅有待处理时显示「M 个待处理」
  - `WorkspacesPage.tsx:754` — 端口「N 个监听中」改为展开后才显示
  - `ArtifactChecklist.tsx:190` — 「刚更新」标记删除

  **文档同步**：`AGENTS.md`（收件箱条改写 + 新增 v3.60 总规则）；`docs/user-guide.md` 七处同步（含修正步进器角标"绿点=已完成"的过期描述）。

  ## 当前状态与未完成事项

  - 构建通过，56 个测试全绿（侧栏修复时为 57，清理 done 相关测试后 56）。HMR 已热更到用户正在运行的 Ccode Dev 窗口，可目视验证。
  - **未完成**：胶囊/收件箱扩展仅停留在建议阶段，未写任何代码。候选按优先级：**产物待核验**（`artifacts.yaml` 登记产物已产出未验收）> **基准前进后的冲突重同步** > **profile/密钥失效** > **接力待发送**（`◈ 提炼接力` 简报已生成未交给下一个 agent）。
  - 够格且已存在的注意力项（勿误删）：待确认、冲突、可合并、drift/校验失败、端口外部进程。

  ## 下一步建议

  - 若继续做胶囊扩展：新来源 = 写一个检测器 + 必要时新增一类 inbox action（现有仅 review/tab/session 三类，经 `runInboxAction` 统一派发，见 `src/store.ts:114`），消费方不动。
  - 数据源现成：产物核验用 `artifacts.yaml` mtime，冲突重同步用 merge-base 比对，均不需要新轮询，从 `terminalRunInputs`/`inboxItems` 镜像的消费模式延伸即可。
  - 任何新指示先进 v3.60 闸门自检："人类决策是否是唯一 unblock 条件"，答不上就不进。
  - 先在运行中的 Dev 窗口目视确认降噪效果无回归，再开新功能。

  ## 环境与约束

  - macOS，系统滚动条设为"始终显示"（经典滚动条）——侧栏 bug 的触发条件，相关回归必须在此设置下复验。
  - Vite dev server + HMR；用户本机 11:51 启动的 Ccode Dev 实例占用默认端口，**不要动它**；会话中拉起的重复实例已退出清理，Safari 测试页与 /tmp 截图已清理。
  - 项目惯例：改动行为必须同步 `AGENTS.md` 与 `docs/user-guide.md`。
  - 收件箱架构硬约束：`InboxItem` 可序列化；**单一写入方**（WorkspacesPage 签名去抖镜像），新来源只能走"聚合并去重"，防止计数漂移。

## 接力说明

- 本简报由 Ccode 从 kimi 会话生成，非完整记忆。
- 源会话的完整上下文仍保留在原 Agent 中；请结合项目文件与 git 历史补全背景。
- 不要假设你知道简报之外的对话细节；不确定时先读代码与相关文件。
