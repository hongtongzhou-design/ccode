# 接力简报（AI 提炼）：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全

> 本简报由 Ccode 用 AI 从 kimi 会话全文提炼（2026-08-11T06:08:49Z），是结构化摘要而非完整记忆。

## 任务信息

- 来源 Agent：kimi
- 项目目录：/Users/tongzhouhong/Documents/Ccode
- 会话标题：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全
- 会话 ID：session_88fe53d5-b142-4320-aa91-00e0af6adf09

## 当前 git 状态

- 分支：main（领先 0 / 落后 0）
- 改动 2 个文件（+164 -0）：
  - ?? .ccode/handoff-20260811T060736Z.md （+78 -0）
  - ?? .ccode/handoff-20260811T060745Z.md （+86 -0）

## 会话要点（AI 提炼）

• ## 任务目标

  本会话依次处理了三件事：

  1. **修复侧栏收缩态 bug**：侧栏收缩后，导航图标的 hover/选中衬底被裁掉一半（图标本身居中未裁），底部控制面板和设置的衬底显示完整。
  2. **第一性原理讨论**：顶部胶囊模块与软件/科研工作的兼容性分析（仅讨论，未实施）。
  3. **工作区与终端页降噪**：按"是否阻塞人的决策"标准盘点全部注意力元素，用户指示「全部执行」后完成清理。

  ## 关键决策与结论

  - **侧栏 bug 根因**：`src/App.css` 给 `.ccode-app-nav` 设了 `scrollbar-gutter: stable`。用户系统滚动条为"始终显示"（经典滚动条），侧栏收缩到 56px 后仍强制预留 ~14px 滚动条沟槽，内容盒只剩 ~29px，而收缩态按钮固定 `w-11`（44px），衬底右半截被 `overflow-x-hidden` 裁掉。已经真实截图像素测量验证（衬底实际 52px，缺口恰为 14pt 沟槽）。
  - **胶囊模块的本质与三不变量**：胶囊是系统唯一注意力路由口，价值 = 信噪比。三不变量：**稀少性**（不阻塞决策的信息进一条、可信度贬一分）、**真实性**（条目对应真实可执行动作）、**可达性**（一键路由到拍板现场，胶囊自身不变成操作界面）。判断标准："哪些事件满足人类决策是唯一 unblock 条件"。
  - **注意力元素的总尺子**："这条信息出现时，是否存在一个只有人能做、且不做就阻塞的动作"。够格的只有：待确认、冲突、可合并、drift/校验失败、端口外部进程。
  - **「已回复」全链路定性为噪音**：回合结束 ≠ 有事要做，每回合都发生；v3.59 否决步进器绿点的理由对全部五处残留成立。
  - **已确立的口径**（写入 AGENTS.md v3.60）：已回复全链路无标记、纯状态不用语义色、瞬态横幅自动消退、禁留无限脉冲、新状态指示进界面前先过"是否阻塞人的决策"闸；计数只出现在胶囊/strip。
  - **胶囊扩展候选的优先级**（仅结论，未实施）：产物待核验 > 基准前进的冲突重同步 > profile 失效 > 接力待发送；进度展示/完成计数/运行状态类需求应继续拒绝。

  ## 已完成的改动（文件 + 要点）

  **侧栏修复（第一部分）**：

  - `src/App.css:364` — `.ccode-app-nav` 移出 `scrollbar-gutter: stable` 规则（保留 `.ccode-app-main`），加注释说明原因。
  - `src/App.tsx:385` — 收缩态导航按钮 `w-11` 改为 `w-full justify-center`，极端情况下衬底自动收窄而不被裁。

  **降噪执行（第三部分，用户指示「全部执行」）**：

  - `src/WorkspacesPage.tsx` — 收件箱移除 `done:` 条目与摘要「N 已回复」，排序变为 冲突 > 待确认 > 可合并；项目导航行副行删「N 个活跃任务」，仅有待处理时显示「M 个待处理」；「✓ 工作区已创建」横幅 10 秒自动消退（setup 失败不自动收）；端口「N 个监听中」改为展开后才显示。
  - `src/TerminalPage.tsx` — 删除标签蓝点与整套 seenDoneRef 已读机制；「工作中」无限 `animate-pulse` 改为有界 `animate-pulse-brief`；「端口段已注入」「可恢复」「上次任务」去 pill 底色和蓝色，降为普通灰字。
  - `src/ProjectRail.tsx` — 工作区行 done 蓝点移除，只留 待确认 > 工作中 > 运行中。
  - `src/notify.ts` — OS 通知只发「待确认」，移除 `NotifyKind`/done 分支（原「已回复，等你查看」在 notify.ts:42-49）。
  - `src/App.tsx` — 清理通知动作里的 done 分支（死代码）。
  - `src/run-overview.ts` — done 不占排序档、不进摘要，`itemRank`/`buildRunOverview` 签名简化。
  - `src/ProjectGroup.tsx:925` — 分组头「N 进行中 / N 待评审」绿/主色点降灰，仅「阻塞」保留语义色。
  - `src/ArtifactChecklist.tsx:190` — 删除「刚更新」标记（文件新旧不是待办）。
  - 两个测试文件同步更新（具体文件名未在记录中给出）。
  - `AGENTS.md` — 收件箱条改写，新增「注意力信噪比总规则（v3.60）」。
  - `docs/user-guide.md` — 七处描述同步，含顺带修正步进器角标"绿点=已完成"的过期描述。

  ## 当前状态与未完成事项

  - 全部改动已完成：构建通过，56 个测试全绿；HMR 已热更到用户开着的 Ccode Dev 窗口，效果可直接查看。（侧栏修复当时为 57 过 0 失败，降噪后测试数为 56，因测试文件同步调整。）
  - **未实施**：胶囊模块扩展仅是讨论结论，四个候选来源（产物待核验、基准前进冲突重同步、profile 失效、接力待发送）均未写代码。
  - **遗留风险**：「可合并」在终端 pill（`TerminalPage.tsx:2946`）、ProjectRail 状态点、步进器角标三处镜像同一信息，讨论中认定为"重复不制造新噪音但增加口径漂移风险"，未做处理。

  ## 下一步建议

  - 若要扩胶囊，按优先级做：产物待核验（用 `artifacts.yaml` 登记产物的 mtime 检测，已产出未验收才进）> 基准前进导致的冲突重同步（merge-base 比对）> profile 三层验证失效/密钥过期 > 接力简报待发送（`◈ 提炼接力` 已生成未交付）。
  - 实现约束：新来源 = 写一个检测器 + 必要时在 `runInboxAction`（`src/store.ts:114`）加一类 action，不动消费方；写入必须走 WorkspacesPage 单一写入方（签名去抖镜像）聚合并去重；从 `terminalRunInputs`/`inboxItems` 镜像消费模式延伸，不需要新轮询。
  - 任何"进度展示、完成计数、运行状态"类需求继续拒绝——胶囊的价值在于它大多数时间是空的。
  - 可选收尾：统一「可合并」三处镜像的口径，降低漂移风险。

  ## 环境与约束

  - 项目：Ccode Dev（AI agent 终端/工作区管理应用），React + TypeScript + Vite + Tailwind，改动经 HMR 即时生效；验证方式 = 构建 + 测试套件。
  - 系统：macOS，系统滚动条设置为"始终显示"（经典滚动条）——侧栏 bug 只在此设置下复现，排查滚动条/布局问题时须记住这一点。
  - 用户本机 11:51 启动的 Ccode Dev 实例未被动过；调试拉起的重复实例（端口占用）与 /tmp 截图、Safari 测试页均已清理。
  - AGENTS.md 现行约定须遵守：收件箱单一写入方、计数只在胶囊/strip、禁无限脉冲、纯状态不用语义色、瞬态横幅自动消退、新注意力元素先过"决策阻塞"闸（v3.60）。
  - 收件箱架构：`InboxItem` 为可序列化条目，action 现有 review/tab/session 三类。
  - 未动的既定状态指示（判定合格，不要误删）：改动页签 +N 绿 pill、对话计数 50+、文件脏点 ●、「有新消息 ↓」、git +N-N。

## 接力说明

- 本简报由 Ccode 从 kimi 会话生成，非完整记忆。
- 源会话的完整上下文仍保留在原 Agent 中；请结合项目文件与 git 历史补全背景。
- 不要假设你知道简报之外的对话细节；不确定时先读代码与相关文件。
