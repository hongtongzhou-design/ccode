# 接力简报（AI 提炼）：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全

> 本简报由 Ccode 用 AI 从 kimi 会话全文提炼（2026-08-11T06:09:08Z），是结构化摘要而非完整记忆。

## 任务信息

- 来源 Agent：kimi
- 项目目录：/Users/tongzhouhong/Documents/Ccode
- 会话标题：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全
- 会话 ID：session_88fe53d5-b142-4320-aa91-00e0af6adf09

## 当前 git 状态

- 分支：main（领先 0 / 落后 0）
- 改动 4 个文件（+339 -0）：
  - ?? .ccode/handoff-20260811T060736Z.md （+78 -0）
  - ?? .ccode/handoff-20260811T060745Z.md （+86 -0）
  - ?? .ccode/handoff-20260811T060849Z.md （+85 -0）
  - ?? .ccode/handoff-20260811T060850Z.md （+90 -0）

## 会话要点（AI 提炼）

• ## 任务目标

  在 Ccode（AI 编程桌面应用，React + Vite 前端）中完成两件事：

  1. 修复侧栏收缩态下图标按钮 hover/选中衬底（背景）右侧被裁切的问题（注意：不是图标被裁，是衬底被裁）。
  2. 系统性清除工作区页与终端页的注意力噪音——以「是否阻塞人的决策」为准入标准，执行用户批准的「全部执行」降噪方案。

  此前的「顶部胶囊模块第一性原理分析」为纯讨论，确立了降噪的理论框架，本身不要求改代码。

  ## 关键决策与结论

  - **裁切根因**：`src/App.css` 给 `.ccode-app-nav` 设了 `scrollbar-gutter: stable`。用户系统滚动条为「始终显示」，侧栏收缩到 56px 后仍强制预留 ~14px 滚动条沟槽，内容盒只剩 ~29px，而按钮固定 `w-11`（44px），衬底右半被 `overflow-x-hidden` 裁掉。已经真实截图像素测量验证（衬底实测 52px，缺口恰为 14pt）。图标居中故不裁；底部控制面板不在滚动容器内故衬底完整。
  - **胶囊模块本质**：系统唯一注意力路由口，回答「此刻有没有事在等我拍板」。三条不变量：稀少性（信噪比）、真实性（条目对应真实可执行动作）、可达性（一键路由到拍板现场，自身不变成操作界面）。准入判据只有一个：**人类决策是唯一 unblock 条件**。
  - **排序框架**：冲突 > 待确认 > 可合并（原为 冲突 > 待确认 > 可合并 > 已回复，「已回复」已整体移除）。
  - **噪音四类定性**：① 「已回复」全链路残留（v3.59 否决步进器绿点的理由对它完全成立：回合结束 ≠ 有事要做）；② 提醒一次就该退场的瞬态反馈；③ 用颜色假扮待办的纯状态；④ 无色跟随式指示（合格，保留）。
  - 「可合并」在终端 pill、ProjectRail 状态点、步进器角标三处与收件箱重复——不制造新噪音但留作口径漂移风险记录，未动。

  ## 已完成的改动（文件 + 要点）

  注意：以下行号为改动时定位用，改动后可能已偏移。

  - `src/App.css:364` — `.ccode-app-nav` 移出 `scrollbar-gutter: stable` 规则（保留 `.ccode-app-main`），加注释说明原因。
  - `src/App.tsx:385` — 收缩态导航按钮 `w-11` 改 `w-full justify-center`；另清理了通知动作里的 done 分支死代码。
  - `src/pages/WorkspacesPage.tsx`（路径按实际目录，约在 1682/1638/754 行附近）— 收件箱移除 `done:` 条目与摘要「N 已回复」；「✓ 工作区已创建」横幅 10 秒自动消退（setup 失败不收）；导航行副行删掉「N 个活跃任务」，仅有待处理时显示「M 个待处理」；端口「N 个监听中」改为展开后才显示。
  - `src/pages/TerminalPage.tsx`（约在 2879-2908/2899/2912/2946/1518 行附近）— 删除标签蓝点及整套 seenDoneRef 已读机制；「工作中」无限 `animate-pulse` 改有界 `animate-pulse-brief`；「端口段已注入」「可恢复」「上次任务」去 pill 底色与蓝色，降为普通灰字。
  - `src/components/ProjectRail.tsx:279-288` — 工作区行 done 蓝点移除，只留 待确认 > 工作中 > 运行中。
  - `src/components/ProjectGroup.tsx:925` — 分组头「N 进行中 / N 待评审」绿/主色点降灰，仅「阻塞」保留语义色。
  - `src/notify.ts:42-49` — OS 通知只发「待确认」，移除 NotifyKind 的 done 分支与「已回复直达评审」链路。
  - `src/run-overview.ts` — done 不占排序档、不进摘要；`itemRank`/`buildRunOverview` 签名简化，两个对应测试文件已同步。
  - `src/components/ArtifactChecklist.tsx:190` — 删除「刚更新」标记。
  - `AGENTS.md` — 收件箱条改写，新增「注意力信噪比总规则（v3.60）」：已回复全链路无标记、纯状态不用语义色、瞬态横幅自动消退、禁留无限脉冲、新状态指示须过「是否阻塞人的决策」闸。
  - `docs/user-guide.md` — 七处描述同步（含顺带修正步进器角标「绿点=已完成」的过期描述）。

  ## 当前状态与未完成事项

  - 构建通过，56 个测试全绿；HMR 已热更到用户开着的 Ccode Dev 窗口，改动已可目视验证。
  - 未做（属讨论结论、非本次承诺）：胶囊扩展的落地建议——产物待核验 > 基准前进的冲突重同步 > profile 失效 > 接力待发送；以及「可合并」三处镜像的归并。
  - 用户尚未回报 dev 窗口里的实际视觉效果确认。

  ## 下一步建议

  - 让用户在 dev 窗口目视确认收缩态衬底与降噪效果；如有残留视觉问题优先复查。
  - 若继续推进胶囊扩展，从「产物待核验」入手：数据现成（`artifacts.yaml` mtime、merge-base 比对），沿 `terminalRunInputs`/`inboxItems` 镜像消费模式延伸即可；接新来源 = 写检测器 + 必要时给 `runInboxAction`（`src/store.ts:114`）加一类 action，不动消费方；写入必须走 WorkspacesPage 签名去抖镜像这一单一写入方。
  - 「进度展示、完成计数、运行状态」类需求应继续拒绝进胶囊。

  ## 环境与约束

  - macOS，系统滚动条设为「始终显示」（经典滚动条）——裁切 bug 的复现前提，回归测试时需保持该设置。
  - Vite dev server + HMR；用户本机 11:51 自启的 Ccode Dev 实例占用端口，不可动它；调试用的重复实例、Safari 测试页、/tmp 截图均已清理。
  - 项目约定（AGENTS.md）：禁无限 `animate-pulse`（用有界 `animate-pulse-brief`）；计数只出现在胶囊/strip；收件箱单一写入方；构建与测试须全绿才算完成（当前 56 测试）。
  - 版本口径：v3.59 否决步进器绿点；v3.60 新增注意力信噪比总规则。

## 接力说明

- 本简报由 Ccode 从 kimi 会话生成，非完整记忆。
- 源会话的完整上下文仍保留在原 Agent 中；请结合项目文件与 git 历史补全背景。
- 不要假设你知道简报之外的对话细节；不确定时先读代码与相关文件。
