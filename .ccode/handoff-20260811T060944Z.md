# 接力简报（AI 提炼）：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全

> 本简报由 Ccode 用 AI 从 kimi 会话全文提炼（2026-08-11T06:09:44Z），是结构化摘要而非完整记忆。

## 任务信息

- 来源 Agent：kimi
- 项目目录：/Users/tongzhouhong/Documents/Ccode
- 会话标题：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全
- 会话 ID：session_88fe53d5-b142-4320-aa91-00e0af6adf09

## 当前 git 状态

- 分支：main（领先 0 / 落后 0）
- 改动 6 个文件（+510 -0）：
  - ?? .ccode/handoff-20260811T060736Z.md （+78 -0）
  - ?? .ccode/handoff-20260811T060745Z.md （+86 -0）
  - ?? .ccode/handoff-20260811T060849Z.md （+85 -0）
  - ?? .ccode/handoff-20260811T060850Z.md （+90 -0）
  - ?? .ccode/handoff-20260811T060908Z.md （+79 -0）
  - ?? .ccode/handoff-20260811T060912Z.md （+92 -0）

## 会话要点（AI 提炼）

• ## 任务目标
  - 初始任务：修复侧栏收缩后，上部导航图标 hover/选中**衬底**被裁切（非图标被裁），底部控制面板/设置衬底正常的问题。
  - 中期讨论：从第一性原理分析顶部胶囊（收件箱）模块与软件/科研工作的兼容性，确立入囊标准。
  - 最终任务（用户指令"全部执行"）：清除工作区页与终端页的注意力噪音——「已回复」全链路残留、无限脉冲、常驻 pill、语义色滥用、重复计数等，并同步文档。此任务**已全部完成**。

  ## 关键决策与结论
  - **侧栏裁切根因**：`src/App.css` 给 `.ccode-app-nav` 设了 `scrollbar-gutter: stable`；用户系统滚动条为"始终显示"，收缩成 56px 后仍预留 ~14px 沟槽，内容盒只剩 ~29px，而按钮固定 `w-11`(44px)，衬底右半被 `overflow-x-hidden` 裁掉。图标居中故未裁；底部控制在滚动容器外故完整（像素测量验证：衬底实际 52px，缺口恰为 14pt）。
  - **胶囊模块本质**：系统唯一的注意力路由口，回答"此刻有没有事在等我拍板"。三条不变量：稀少性（信噪比，不阻塞决策的信息进一条可信度贬一分）、真实性（条目须对应当刻可执行动作）、可达性（一键路由到拍板现场，胶囊自身不变成操作界面）。
  - **入囊判定标准**：该信息出现时，是否存在只有人能做、且不做就阻塞的动作。不够格的典型：「已回复」、运行中数量、token 用量、端口监听列表、纯状态。
  - **排序框架**：冲突 > 待确认 > 可合并（不动就烂 > 动了才走 > 可动可不动）。
  - **历史背书**：「已回复」绿点、侧栏数字徽标、状态点撞语义此前已被否决三次；v3.59 否决步进器绿点的理由（回合结束 ≠ 有事要做）对五处「已回复」残留同等成立。
  - 架构事实：`InboxItem` 可序列化 + `runInboxAction` 统一派发（`src/store.ts:114`，action 现有 review/tab/session 三类）；收件箱单一写入方（WorkspacesPage 签名去抖镜像），新来源只能走聚合并去重。

  ## 已完成的改动（文件 + 要点）
  **侧栏修复（先已完成）**
  - `src/App.css:364` — `.ccode-app-nav` 移出 `scrollbar-gutter: stable` 规则（保留 `.ccode-app-main`），加注释说明。
  - `src/App.tsx:385` — 收缩态导航按钮 `w-11` 改为 `w-full justify-center`。

  **「已回复」全链路清除（终端标签/项目区/收件箱/通知/排序）**
  - `WorkspacesPage.tsx` — 收件箱 `done:` 条目与摘要「N 已回复」移除；排序变为 冲突 > 待确认 > 可合并。
  - `TerminalPage.tsx`（约 2879-2908）— 标签蓝点与整套 seenDoneRef 已读机制删除。
  - `ProjectRail.tsx`（约 279-288）— 工作区行 done 蓝点移除，只留 待确认 > 工作中 > 运行中。
  - `notify.ts`（约 42-49）与 `App.tsx` — OS 通知只发「待确认」；`NotifyKind`/done 分支及「已回复直达评审」链路（含 App.tsx 死代码）移除。
  - `run-overview.ts` — done 不占排序档、不进摘要；`itemRank`/`buildRunOverview` 签名简化（两个测试文件已同步）。

  **其余降噪**
  - `TerminalPage.tsx`：标签「工作中」无限 `animate-pulse`（约 2899）→ 有界 `animate-pulse-brief`；「端口段已注入」（约 1518）、「可恢复」徽标（约 2912）、「上次任务」去 pill 底色降为普通灰字。
  - `WorkspacesPage.tsx`：「✓ 工作区已创建」横幅（约 1682）10 秒自动消退（setup 失败不自动收）；项目导航行副行（约 1638）删「N 个活跃任务」，仅有待处理时显示「M 个待处理」；端口「N 个监听中」（约 754）改为展开后才显示。
  - `ProjectGroup.tsx`（约 925）— 分组头「N 进行中 / N 待评审」绿/主色点降灰，仅「阻塞」保留语义色。
  - `ArtifactChecklist.tsx`（约 190）—「刚更新」标记删除。

  **文档同步**
  - `AGENTS.md` — 收件箱条改写，新增「注意力信噪比总规则（v3.60）」：已回复全链路无标记、纯状态不用语义色、瞬态横幅自动消退、禁留无限脉冲、新状态指示先过「是否阻塞人的决策」闸。
  - `docs/user-guide.md` — 七处描述同步，含顺带修正步进器角标"绿点=已完成"的过期描述。

  ## 当前状态与未完成事项
  - 构建通过，测试 56 个全绿（降噪后数量）；HMR 已热更到用户开着的 Ccode Dev 窗口。
  - 未完成事项：
    - 用户尚未在 dev 窗口人工目检降噪后的实际视觉效果。
    - 此前讨论的胶囊扩展（产物待核验、冲突重同步、profile 失效、接力待发送）仅为分析建议，**未实施**。

  ## 下一步建议
  - 请用户在 Ccode Dev 窗口确认终端标签、项目区、收件箱的日常噪音确实减少。
  - 若要扩展胶囊来源，按既定优先级：产物待核验（基于 `artifacts.yaml` mtime，无需新轮询）> 基准已前进的冲突重同步 > profile 三层验证失效 > 接力待发送；从 `terminalRunInputs`/`inboxItems` 镜像消费模式延伸，遵守单一写入方约束。
  - 任何"进度展示、完成计数、运行状态"类入囊需求继续拒绝——胶囊的价值在于大多数时间是空的。

  ## 环境与约束
  - macOS，系统滚动条"始终显示"（经典滚动条）——侧栏裁切 bug 的触发条件，复现/回归需注意此设置。
  - 项目为 Vite + React（TS），验证手段为构建 + 测试套件；端口被用户本机 Ccode Dev 实例（11:51 启动）占用，该实例未动过；助手拉起的重复实例已退出清理，Safari 测试页与 /tmp 截图已清理。
  - 行为变更须同步 `AGENTS.md` 与 `docs/user-guide.md`；新注意力元素一律按 v3.60 规则过「是否阻塞人的决策」闸。

## 接力说明

- 本简报由 Ccode 从 kimi 会话生成，非完整记忆。
- 源会话的完整上下文仍保留在原 Agent 中；请结合项目文件与 git 历史补全背景。
- 不要假设你知道简报之外的对话细节；不确定时先读代码与相关文件。
