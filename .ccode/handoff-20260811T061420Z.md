# 接力简报（AI 提炼）：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全

> 本简报由 Ccode 用 AI 从 kimi 会话全文提炼（2026-08-11T06:14:20Z），是结构化摘要而非完整记忆。

## 任务信息

- 来源 Agent：kimi
- 项目目录：/Users/tongzhouhong/Documents/Ccode
- 会话标题：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全
- 会话 ID：session_88fe53d5-b142-4320-aa91-00e0af6adf09

## 当前 git 状态

- 分支：main（领先 0 / 落后 0）
- 改动 12 个文件（+987 -0）：
  - ?? .ccode/handoff-20260811T060736Z.md （+78 -0）
  - ?? .ccode/handoff-20260811T060745Z.md （+86 -0）
  - ?? .ccode/handoff-20260811T060849Z.md （+85 -0）
  - ?? .ccode/handoff-20260811T060850Z.md （+90 -0）
  - ?? .ccode/handoff-20260811T060908Z.md （+79 -0）
  - ?? .ccode/handoff-20260811T060912Z.md （+92 -0）
  - ?? .ccode/handoff-20260811T060944Z.md （+80 -0）
  - ?? .ccode/handoff-20260811T060948Z.md （+96 -0）
  - ?? .ccode/handoff-20260811T061108Z.md （+84 -0）
  - ?? .ccode/handoff-20260811T061121Z.md （+85 -0）
  - ?? .ccode/handoff-20260811T061356Z.md （+66 -0）
  - ?? .ccode/handoff-20260811T061359Z.md （+66 -0）

## 会话要点（AI 提炼）

• ## 任务目标

  会话经历三个阶段，前两阶段已完成，第三阶段待执行：

  1. **已完成**：修复侧栏收缩后 hover/选中衬底被裁切的问题。
  2. **已完成**：按「是否阻塞人的决策」标准，清除工作区页与终端页的注意力噪音（「已回复」全链路、无限脉冲、语义色滥用等）。
  3. **未完成（当前任务）**：执行四个收件箱新来源，优先级已定：**产物待核验 > 基准前进的冲突重同步 > profile 失效 > 接力待发送**；其中「接力待发送」附带一个 UX 改造要求：`◈ 提炼接力` 生成简报目前会一直停留在等待页面、重新点开后会重新生成——要改成**生成过程放后台，生成好了之后显示**，重复打开不重新生成。

  ## 关键决策与结论

  - **顶部胶囊的本质 = 系统唯一注意力路由口**，三条不变量：稀少性（价值=信噪比，不阻塞决策的信息进一条可信度贬一分）、真实性（条目必须对应当刻可执行的动作）、可达性（每条一键路由到拍板现场，胶囊自身不变成操作界面）。
  - **入选标准**：事件必须满足「人类决策是唯一 unblock 条件」。排序框架：冲突 > 待确认 > 可合并 > （已回复已废除），本质是「不动就烂 > 动了才走 > 可动可不动」。
  - **「已回复」类信号已被否决**（先例：v3.59 步进器绿点），理由是回合结束 ≠ 有事要做、每轮都发生 = 纯噪音。v3.60 新增总规则：已回复全链路无标记、纯状态不用语义色、瞬态横幅自动消退、无限脉冲禁留；新状态指示进界面前先过「是否阻塞人的决策」闸。
  - **架构结论**：接新来源 = 写一个检测器 + 必要时加一类 action，不动消费方。复用现成数据（`artifacts.yaml` mtime、merge-base 比对），不需要新轮询，从 `terminalRunInputs`/`inboxItems` 镜像的消费模式延伸即可。

  ## 已完成的改动（文件 + 要点）

  **侧栏衬底修复**（根因：`scrollbar-gutter: stable` 在经典滚动条系统上给 56px 收缩侧栏强占 ~14px 沟槽，44px 按钮衬底右半被 `overflow-x-hidden` 裁掉）：

  - `src/App.css:364` — `.ccode-app-nav` 移出 `scrollbar-gutter: stable` 规则（保留 `.ccode-app-main`），加注释。
  - `src/App.tsx:385` — 收缩态导航按钮 `w-11` 改 `w-full justify-center`。

  **降噪改动**：

  - `src/WorkspacesPage.tsx` — 收件箱 `done:` 条目与「N 已回复」摘要移除，排序改为 冲突 > 待确认 > 可合并；「✓ 工作区已创建」横幅 10 秒自动消退（setup 失败不自动收）；项目导航行副行删「N 个活跃任务」，仅有待处理时显示「M 个待处理」（~:1638）；端口「N 个监听中」改为展开后才显示（~:754）。
  - `src/TerminalPage.tsx` — 标签蓝点与整套 seenDoneRef 已读机制删除（原 :2879-2908）；「工作中」无限 `animate-pulse` 改有界 `animate-pulse-brief`；「端口段已注入」「可恢复」「上次任务」去 pill 底色与蓝色，降为普通灰字。
  - `src/ProjectRail.tsx` — 工作区行 done 蓝点移除（原 :279-288），只留 待确认 > 工作中 > 运行中。
  - `src/ProjectGroup.tsx:925` — 分组头「N 进行中 / N 待评审」的绿/主色点降灰，仅「阻塞」保留语义色。
  - `src/notify.ts` — 只发「待确认」；`NotifyKind`/done 分支与「已回复直达评审」链路移除。
  - `src/App.tsx` — 通知动作里的 done 死代码分支清理。
  - `src/run-overview.ts` — done 不占排序档、不进摘要；`itemRank`/`buildRunOverview` 签名简化；两个对应测试文件同步更新。
  - `src/ArtifactChecklist.tsx:190` — 「刚更新」标记删除。
  - 文档：`AGENTS.md` 收件箱条改写 + 新增「注意力信噪比总规则（v3.60）」；`docs/user-guide.md` 七处同步（含修正步进器角标"绿点=已完成"的过期描述）。

  ## 当前状态与未完成事项

  - 构建通过，56 个测试全绿；HMR 已热更到用户开着的 Ccode Dev 窗口。前两阶段任务全部收尾。
  - 第三阶段（四个收件箱来源 + 接力简报后台生成）**尚未动手**。各来源的判定口径已在讨论中明确：
    - **产物待核验**：`artifacts.yaml` 登记的预期产物已产出但无人验收（图渲染完、data-clean 跑完、quarto 出 PDF），用 mtime 检测。
    - **基准前进的冲突重同步**：基准已前进、冲突两侧需重新同步，用 merge-base 比对检测。
    - **profile 失效**：profile 三层验证失败 / 密钥过期，agent 起不来。
    - **接力待发送**：简报已生成但未交给下一个 agent；同时改造生成流程为后台执行、完成后展示、重开不重生成。
  - 明确**不做**：运行中数量、token 用量、端口监听列表、进度展示、完成计数等纯状态，一律不进胶囊。

  ## 下一步建议

  1. 先读 `src/store.ts:114` 附近的 `InboxItem`/`runInboxAction` 派发结构与 WorkspacesPage 的签名去抖镜像（单一写入方），确定新检测器的挂载点。
  2. 按优先级实现：产物待核验 → 冲突重同步 → profile 失效 → 接力待发送；每类来源 = 检测器 + 聚合并去重写入镜像，必要时加一类 action。
  3. 定位 `◈ 提炼接力` 的生成入口与等待页组件，改为后台任务模式：生成中不阻塞页面，完成后展示结果，重复打开复用已生成简报。
  4. 每步完成后跑构建与测试保持全绿，并同步 `AGENTS.md` 与 `docs/user-guide.md`（该项目有改动即同步文档的惯例）。

  ## 环境与约束

  - macOS，用户系统滚动条为「始终显示」（经典滚动条）——这是侧栏 bug 的触发条件，涉及滚动容器改动时注意回归。
  - 用户本机 11:51 自启的 Ccode Dev 实例占用着 dev 端口，**不要动它**；改动通过 HMR 生效，重复拉实例会因端口占用退出。
  - 会话记录只出现相对路径（`src/...`、`AGENTS.md`、`docs/user-guide.md`），新会话需先定位项目根目录。
  - 项目惯例：AGENTS.md 规则带版本号（当前到 v3.60）；无限 `animate-pulse` 视为注意力消耗；计数只出现在胶囊/strip；收件箱镜像单一写入方（WorkspacesPage 签名去抖）。
  - 收尾要求：构建通过、测试全绿、文档同步，三样齐才算完成。

## 接力说明

- 本简报由 Ccode 从 kimi 会话生成，非完整记忆。
- 源会话的完整上下文仍保留在原 Agent 中；请结合项目文件与 git 历史补全背景。
- 不要假设你知道简报之外的对话细节；不确定时先读代码与相关文件。
