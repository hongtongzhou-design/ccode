# 接力简报（AI 提炼）：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全

> 本简报由 Ccode 用 AI 从 kimi 会话全文提炼（2026-08-11T06:11:21Z），是结构化摘要而非完整记忆。

## 任务信息

- 来源 Agent：kimi
- 项目目录：/Users/tongzhouhong/Documents/Ccode
- 会话标题：侧边栏收缩后 底部的控制面板和设置显示完全 鼠标放在上面显示的轮廓 上面的一系列图标显示不完全
- 会话 ID：session_88fe53d5-b142-4320-aa91-00e0af6adf09

## 当前 git 状态

- 分支：main（领先 0 / 落后 0）
- 改动 9 个文件（+770 -0）：
  - ?? .ccode/handoff-20260811T060736Z.md （+78 -0）
  - ?? .ccode/handoff-20260811T060745Z.md （+86 -0）
  - ?? .ccode/handoff-20260811T060849Z.md （+85 -0）
  - ?? .ccode/handoff-20260811T060850Z.md （+90 -0）
  - ?? .ccode/handoff-20260811T060908Z.md （+79 -0）
  - ?? .ccode/handoff-20260811T060912Z.md （+92 -0）
  - ?? .ccode/handoff-20260811T060944Z.md （+80 -0）
  - ?? .ccode/handoff-20260811T060948Z.md （+96 -0）
  - ?? .ccode/handoff-20260811T061108Z.md （+84 -0）

## 会话要点（AI 提炼）

• ## 任务目标

  本会话按顺序处理了三件事：

  1. 修复侧栏收缩后 hover/选中衬底被裁切的问题（不是图标被裁）。
  2. 从第一性原理分析顶部胶囊模块与软件/科研工作的兼容性（纯分析，未写码）。
  3. 盘点工作区页与终端页的注意力噪音，并在用户指示「全部执行」后完成全部降噪改动。

  ## 关键决策与结论

  - **胶囊模块的本质**：系统里唯一的注意力路由口，回答「此刻有没有事在等我拍板」。三条不变量：稀少性（信噪比，不阻塞决策的信息不进）、真实性（条目必须对应当刻可执行的动作）、可达性（一键路由到拍板现场，胶囊自身不变成操作界面）。
  - **统一的筛选标准**：任何状态指示进界面前先过「是否阻塞人的决策」这一闸。排序框架：冲突 > 待确认 > 可合并，即「不动就烂 > 动了才走 > 可动可不动 > 已经动了」。
  - **「已回复」定性为纯噪音**：回合结束 ≠ 有事要做，每轮都发生。其五处残留（终端标签蓝点、ProjectRail 蓝点、收件箱条目、run-overview 计数、OS 通知）全部清除，并连带删除 seenDoneRef 已读机制（真信号不需要已读）。
  - **纯状态不得用语义色**：分组头「进行中/待评审」降灰，仅「阻塞」保留语义色；「端口段已注入」「可恢复」「上次任务」去 pill 降为灰字。
  - **瞬态反馈自动消退**：「✓ 工作区已创建」横幅 10 秒自收（setup 失败除外）；无限 `animate-pulse` 禁留，统一改有界 `animate-pulse-brief`。
  - **「可合并」三处镜像（终端 pill、ProjectRail 状态点、收件箱）有意保留**：重复不制造新噪音，但已记录口径漂移风险。
  - **侧栏 Bug 根因**：`scrollbar-gutter: stable` + 用户系统滚动条「始终显示」→ 56px 收缩侧栏被强制预留 ~14px 沟槽，44px 按钮衬底被 `overflow-x-hidden` 裁掉（截图像素测量验证：衬底实宽 52px，缺口恰为 14pt）。

  ## 已完成的改动（文件 + 要点）

  注意：以下行号是改动前定位用的，改后可能已漂移。

  - `src/App.css:364` — `.ccode-app-nav` 移出 `scrollbar-gutter: stable` 规则（保留 `.ccode-app-main`），附注释说明原因。
  - `src/App.tsx:385` — 收缩态导航按钮 `w-11` 改 `w-full justify-center`；另清理了通知动作里已成死代码的 done 分支。
  - `WorkspacesPage.tsx` — 收件箱移除 `done:` 条目与「N 已回复」摘要，排序变为 冲突 > 待确认 > 可合并；`:1682` 创建横幅 10 秒自动消退；`:1638` 副行删「N 个活跃任务」，仅有待处理时显示「M 个待处理」；`:754` 端口「N 个监听中」改为展开后才显示。
  - `TerminalPage.tsx` — `:2879-2908` 标签蓝点与 seenDoneRef 机制删除；`:2899` 无限脉冲改 `animate-pulse-brief`；`:1518` 端口段 pill、`:2912` 可恢复徽标、「上次任务」降为灰字。
  - `ProjectRail.tsx:279-288` — 工作区行 done 蓝点移除，只剩 待确认 > 工作中 > 运行中。
  - `ProjectGroup.tsx:925` — 分组头进行中/待评审点降灰。
  - `ArtifactChecklist.tsx:190` — 「刚更新」标记删除。
  - `run-overview.ts` — done 不占排序档、不进摘要；`itemRank`/`buildRunOverview` 签名简化；两个对应测试文件已同步。
  - `notify.ts:42-49` — 只发「待确认」；`NotifyKind` 的 done 分支与「已回复直达评审」链路移除。
  - `AGENTS.md` — 收件箱条改写，新增「注意力信噪比总规则（v3.60）」。
  - `docs/user-guide.md` — 七处描述同步（含修正步进器角标「绿点=已完成」的过期描述）。

  ## 当前状态与未完成事项

  - 三项任务全部完成：构建通过，56 个测试全绿（改动前为 57，因 done 链路删除同步过测试），HMR 已热更到用户开着的 Ccode Dev 窗口。
  - **未实施**（仅停留在分析建议）：胶囊模块的新来源扩展，优先级已排定为 产物待核验 > 基准前进的冲突重同步 > profile 失效 > 接力待发送。
  - 科研兼容候选（未实施）：`artifacts.yaml` 登记产物已产出待验收、产物逾期未产出（agent 疑似卡住）、`◈ 提炼接力` 简报已生成待发送；`notes/inbox.md` 笔记整理被判定为不够格。

  ## 下一步建议

  - 若继续扩展胶囊：按上述优先级做，检测逻辑复用现成数据（`artifacts.yaml` mtime、merge-base 比对），不新增轮询；从 `terminalRunInputs`/`inboxItems` 镜像的消费模式延伸。
  - 接新来源的方式：写一个检测器 + 必要时在 `runInboxAction`（`src/store.ts:114`，现有 review/tab/session 三类 action）加一类，不动消费方；必须走 WorkspacesPage 单一写入方聚合去重路径，防止计数漂移。
  - 任何新状态指示进界面前，先过「是否阻塞人的决策」闸（已写入 AGENTS.md v3.60 规则）。

  ## 环境与约束

  - macOS，系统滚动条设为「始终显示」（经典滚动条）——侧栏 Bug 的复现前提，回归测试时注意。
  - 项目为 Vite + React/TS；dev server 带 HMR。**用户自己 11:51 启动的 Ccode Dev 实例占着端口，不要动它**；重复拉起实例会因端口占用退出。调试用的 Safari 测试页与 /tmp 截图已清理。
  - 有构建与测试命令；AGENTS.md 是强约束文档，改动 UI 状态指示须同步其中的约定（当前版本号口径 v3.60）。
  - 收件箱架构约束：`InboxItem` 可序列化、`runInboxAction` 统一派发、单一写入方（WorkspacesPage 签名去抖镜像）。

## 接力说明

- 本简报由 Ccode 从 kimi 会话生成，非完整记忆。
- 源会话的完整上下文仍保留在原 Agent 中；请结合项目文件与 git 历史补全背景。
- 不要假设你知道简报之外的对话细节；不确定时先读代码与相关文件。
