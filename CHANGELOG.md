# 更新日志

本项目按[语义化版本](https://semver.org/lang/zh-CN/)管理。架构级决策记录见 [docs/architecture.md](docs/architecture.md) §10。

## Unreleased（未发布）

**工作区**

- 评审面板：任务 diff 文件清单 + 逐文件彩色 diff；冲突闭环——并入主分支、两侧内容预览、逐文件/一键选边、◈ AI 选侧建议（确认后执行）、完成提交，简单冲突不出页面
- 合并拆分：「合并（保留工作区）」与「合并并归档」（拆分按钮 + 下拉）；「已合并」状态 pill（继续提交自动消失）；有冲突时评审按钮亮警示色
- 合并健康检查前置主仓库状态（未提交/不在基准分支，按钮提前灰并说明原因）；合并被拒列出脏文件；归档/删除/合并后，留在被移除工作树里的终端自动 cd 回主仓库
- 文件树/预览标注路径归属（主仓库黄点/分支绿点）+ 主项目⇄多分支切换下拉；主仓库文件保存警示色 + 二次确认（防误改主项目）

**终端**

- 专注模式重构：只剩终端，标签列表与集成动作菜单移到 App 侧栏（收缩时纯符号、悬浮显示名称）
- 真实 cwd 跟踪：文件树/git 面板跟随 shell 内 cd（macOS/Linux）
- 预览编辑器：稳定文档模型（不随切换换文件）、外部改写自动刷新、切换瞬间旧内容立即失效
- git 提交/推送成功 toast（主题绿、带提交号，自动淡出）

**会话**

- ⇗ 外部终端恢复（macOS Ghostty/iTerm/Terminal、Windows cmd、Linux 常见终端）+ ⧉ 复制恢复命令（cc-switch 风格，含 cd 项目目录）；恢复入口合并为「在终端恢复 ▾」
- 回放头部按钮精简（三恢复入口合并下拉）+ 窄窗口换行适配

**配置**

- 组头只显示版本号 + 更新三态：新版（可点升级）/ 已更新 / 更新（brew info / npm view 检查，进程内缓存）
- 行内去掉 API 地址、改为显示配置模型（可 4 个 +N）；编辑表单提示各 agent 模型切换页上限（超出黄色警告）

**设置**

- 新增「外部终端」首选选项（⇗ 外部恢复用）

**修复与安全**

- 打包版 kimi 检测不到（`~/.kimi-code/bin` 未在二进制解析候选目录）
- 移除「预览跟随」（同名文件跨仓库静默切换曾导致误改主仓库，教训已记入 AGENTS.md）
- 侧栏底部收缩按钮并入 Ccode 图标（点击图标即收缩/展开）
- 结果横幅统一灰底 + ✓/✗ 语义色文字（去除整块绿/红底）；状态 pill 统一灰底 + 语义色小圆点

## v0.1.1（首个发布版本）

**模块**

- 配置中心：六 agent（Claude Code/Codex/Gemini/Qwen/OpenCode/Kimi）多配置管理、多模型切换、CLI 安装/更新、连接测试
- 工作区：git worktree 任务隔离、`.ccode/settings.toml` 自动化（files-to-copy/setup/archive 脚本/端口段）、评审流（任务 diff/健康状态/本地合并/gh PR）
- 终端：多标签 xterm.js、agent 退出回落 shell、会话一键恢复、Monaco 文件预览编辑、git 改动面板、⌘F 输出搜索、◈ AI 提交信息
- 会话：六 CLI 会话解析回放（含外部运行会话感知）、pin 快照/标签/归档/搜索/批量删除、◈ AI 摘要、Markdown 导出
- 技能：Skills 统一库 + 六 CLI 分发、目录/ZIP/GitHub 导入、ZIP 导出、分类管理
- 统计：token/费用统计（官方价、$/¥ 切换）、agent 占比进度条
- 设置：七套深色主题、终端字体/调色板、AI 专用配置、自动更新、诊断日志

**性能**

- 前端分包：首屏 4.7MB → 224KB；终端页 memo 化与轮询门控；输出合帧
- list_repos 60s 缓存

**修复与安全**

- 工作区起点从 origin 主分支改为本地主分支（未推送提交不再丢失）
- 删除保护 canonicalize 双校验（堵符号链接绕过）；codex 默认沙箱（交互 workspace-write / 无头 read-only）
- CLI 二进制解析 GUI 短 PATH 兜底
- macOS 暂未签名公证：首次打开需 `xattr -cr /Applications/Ccode.app`
