# 更新日志

本项目按[语义化版本](https://semver.org/lang/zh-CN/)管理。架构级决策记录见 [docs/architecture.md](docs/architecture.md) §10。

> 说明：v0.1.0 之后的演进长期只记在 architecture.md §10 的决策记录里（v0.1–v3.84），本文件未同步。
> 补记历史版本需要可靠的版本切分依据，缺依据不臆造；下面从「未发布」段起恢复逐条记录。

## 未发布

- **定时巡检弹层不再露出一条白页头**：打开弹层时整页都会压暗，顶部「项目」那一行不会单独亮着。

- **无流程科研和编程页也能建定时任务**：不挂研究流程的科研项目，定时任务在文献雷达下面（以前只藏在项目设置里，而这页没有 ⋯）。编程项目在右侧会话栏下面，和办公一样。

- **可以自己建定时任务**：项目里「＋ 定时巡检」除了文献雷达，可以选已有的巡检技能，或「新建巡检技能」——写一句要做什么，跟 AI 把技能写出来，你确认后才保存并开始定时。数据清洗这类一次性技能不再出现在这个下拉里。

- **对话搜索点进去会停在命中句**：搜到正文后点开会话，会打开那一句附近并标出来，而不是停在末尾。Kimi 那种拆成很多行的回复也能对上。

- **项目页内容井再抬一点**：文档、笔记、文献这些底栏仍跟着画布走，只比页面底稍亮一点，浅色能看成一层，不会再漂成一块白纸。

- **示例课题改成 15 分钟精读环**：创建后检索步已经筛完，打开就到「文献精读与笔记」，主按钮「开读这一篇」进笔记｜PDF｜终端。划一段写进笔记即可；完整五步留给自己的课题。步骤与内置「英文综述」模板同一份（改模板后导出 JSON），不再另写一套旧简报。
- **开步确认后 Agent 直接跑**：弹层只摆本步产物、上一步接到的文件、将用的 Agent（记住过则一行摘要，点「更换」再选）。可选准备不再算「待做」。点确认即拉起。没有连接会请你先加一个。任务书默认收起。
- **合并后自动登记提货单**：本步声明的产物在项目根扫得到、且没进 git 的，合并成功会写入 `artifacts.yaml`。下一步开步弹层用芯片列出接到的文件（没有的标「还没有」）。
- **雷达待处理点标题就开读**：收件箱文献行改成论文标题；有 PDF 则加入精读清单并打开沉浸阅读，没有则跳到雷达里那一条。
- **接力简报按科研现场写**：提炼小节改为课题与步骤、纳入/精读、拍板、下一步要读的文件；不编造没出现过的文献。合并后「去下一步」会把评审简报写进下一步草稿。

- **运行页聊天还没开始时不再说「等待会话文件」**：那是内部同步状态，人看起来像卡了。中间那句「从一个问题开始」去掉，改到输入框占位「问一个问题…」；「将在这个目录开始」收进输入框左下角（点它可以换文件夹）。聊天画面不加点阵。还没有会话时不摆「＋ 新建 / 历史」。

- **运行页未启动空态不再像登录卡**：中间那张卡不再重复写 Codex / 配置名，改成「将在这个目录启动」（点它可以换文件夹）。底下状态栏未启动时只留小圆点和目录，模型芯片等跑起来再出现。空白画布铺了淡点阵，卡片也不再丢大阴影。

- **列表「显示全部」可收起**：文献 / 笔记 / 数据（以及本项目会话、雷达新命中）点开全部后底部改成「收起」。按钮文字在框里居中——缩进改写在按钮外面，不再把左内边距挤掉。

- **Excel 预览能看全列、认合并单元格**：办公弹层里的表格加宽，左右滑动看后面的列（不再把「输出」裁掉）。xlsx 的合并表头按合并格显示，不再留下一堆空格。点格子仍在顶上看全文。

- **无流程科研和编程也能「＋ 新对话」**：办公侧栏里那个入口，现在另外两种项目也有。点了在项目根开聊；⌘ / Ctrl 点可重选 Agent 和配置。

- **文献「已读」不再用那块绿**：改成和「精读 / 查看」同一套灰底字色，深色浅色都跟着主题走，不再单独跳一粒绿胶囊。

- **项目页文档区不再垫一张白纸**：办公文档、笔记、文献、雷达、编程工作树、定时任务这些底栏跟着画布走，换浅色/深色/自定义主题都会一起变，不再漂成一块白。

- **运行页文件树单击只展开收起**：点文件夹或前面的箭头都在原地开合，连点也不会钻进去。要进去当浏览根，悬停该行点「进入」。

- **办公文档「继续上次」不再垫一块白板**：和下面的文件列表同一底色，只留一行小标题。文件行「进行中」只在 Agent 正对着这份文件跑（或等你确认）时出现；打开过预览、项目里另有对话，都不会再标。

- **运行页左侧「项目」能看见同时在跑的几个项目**：不限科研/编程/办公。正在跑或等你确认的已添加项目会列出来，当前标签那一项置顶。文件树仍只显示当前这一个文件夹；编程工作树和办公文档不在左侧再铺一棵树。

- **可以自定义主题**：设置 → 外观在十四套预设下面有一张「自定义」。点左栏、画布、强调三块色卡改颜色，文字、卡片、线条自动生成。「从当前主题拷入」在色卡右边。「另存为色卡」可起名存成可点的色卡（最多 12 套，双击改名，悬停 ✕ 删除）。⌘K 里也有「主题：自定义」和已存色卡。

- **项目会话「继续」改成悬停符号**：行上空闲只显示标题和日期，不再常驻「继续」二字。悬停时 ▶ 与 ⚑ 保留、✎ 重命名并排，点 ▶ 在终端接着聊。点标题仍打开对话页。

- **Mac 全屏时标题栏不再空一块**：窗口模式左边仍给红绿灯让位；进全屏三个按钮收起后，项目名贴左边，不再留空。

- **应用更新提示不再只藏在设置页**：启动检查到新版本后，顶栏「待处理」和工作台「待你处理」会出现「更新」条目，点「去安装」跳到设置页对应分区。检查失败会说明原因，不再写成「未发现新版本」。下载时显示进度。开发预览不检查。发版必须提高应用版本号，否则已安装用户收不到更新。

- **添加项目后直接打开该项目**：不会再停在刚才看的那一个。科研项目仍会先弹出选模板。

- **沉浸阅读 PDF 可用滚轮和触控板缩放**：按住 ⌘/Ctrl 再滚轮，或触控板双指捏合，对准指针连续缩放（手势过程不再一顿一顿重绘页面）。放大很多时预览下方不再涂黑。顶栏 − / ＋ /「适配宽度」还在；普通滚动仍然是翻页。

- **课题主题改到选模板时再填**：添加项目只问名字和工作方式。科研项目弹出「选择研究流程模板」时，顶部可以写课题主题（给 Agent 看、写进 TASK.md）。编程和办公不会再看到这一项。

- **编程页 Git / GitHub 环**：主按钮改成「从 <基准> 开工」，本地已有同名分支会先确认，不会悄悄挂上去；也可从已有本地或远程分支建工作树。身份行显示远程、可连接 origin（https/ssh）。芯片区分「待合入 / 落后基准」和「未推送 / 远程有更新」。可用 GitHub Desktop 打开这一棵工作树（不要打开主仓去找 Agent 的改动）。github.com 上已推送的树可打开 Pull Request：点下去先变成「打开中…」；有已开的 PR 且 `gh` 能秒开就走网页，否则马上打开 compare，不再先卡在登录探测上。

- **无流程科研页去掉右上角 ⋯**：不挂研究流程时身份行不再放项目菜单（编辑流程 / 项目设置 / 历史 / 移除）。重命名和移除仍在左侧项目栏右键。

- **项目会话栏三页对齐**：无流程科研、编程、办公都是左主区右会话，会话从项目名那一行顶上对齐。收起后主区铺满整行，展开按钮在项目名右侧（不再在按钮下面留一列空）。

- **工作台最近项目不再每行写「Codex」**：跳到 Codex 客户端改成悬停才出现的外开图标，右边只留「未添加」和时间。主卡底栏仍是「在 Codex 打开」。

- **项目区会话可点「继续」进 Agent**：点标题仍打开对话页回放；悬停 ▶ 在终端接上这条会话。已经在跑则切到那个标签。源文件没了且没保留的不能继续。

- **工作台最近项目会列出刚添加的项目**：不再只扫 Agent 会话里的 git 仓库。办公项目、还没开过对话的新项目也会出现（例如「AI应用教程」）。最近项目和最近对话最多各显示 10 条。

- **无流程科研页会话改可收侧栏**：不挂研究流程时，本项目会话移到右侧，和编程 / 办公页一样可以收起。文献、笔记、数据、雷达仍在左边。

- **项目页 Markdown 预览能显示本地图片**：办公/笔记弹层预览不再把相对路径图片当成网页去拉（那会裂成问号）。改成和运行页阅读一样，走本地图片通道。

- **项目页左侧按工作方式分组**：科研 / 编程 / 办公分列，还没添加的仓库在最下。组内仍按最近打开排，行上不再重复写「科研」「办公」。

- **办公页文档行与右侧栏收口**：文件名过长截断，不再压住日期和「进行中」。悬停「问 AI / 系统打开 / 显示」单行出现、换掉日期，不再竖着拆字。右侧对话 + 定时任务改成和编程页一样可收起的侧栏。文件夹默认收起，记住你上次展开了哪些；搜索时仍会展开。对话标题里的绝对路径改成文件名，不再显示成「看这份文件：/」。

- **本项目会话不再列出 Ccode 拉起的对话**：雷达解读、定时巡检、问 AI（「看这份文件」）、沉浸阅读注入、接力简报都不进项目页会话列表。问 AI 和阅读会话仍在对话页，不进「内部 AI」。

- **雷达解读和定时巡检不进本项目会话**：无头 AI 写出来的 CLI 会话归到对话页「内部 AI」，项目页会话列表不再出现。定时巡检仍按项目计 token，不会把整个项目的交互会话标成内部。

- **项目页按文件夹分级**：办公文档、以及不挂研究流程的文献 / 笔记 / 数据，按真实目录做成可折叠的树（夹里还可以再套夹，本层文件跟子文件夹并列）。全在同一层时仍是平铺。搜索时自动展开。

- **雷达「解读」改成学术快筛并落盘**：进行中显示「解读进行中…」，不再是一块白板。解读按五节写（问题与动机 / 做法与对象 / 摘要声称的结果 / 局限与未写明 / 对本课题的用处），只根据标题和摘要，不编造。结果写入项目 `.ccode/watch-explains.json`，下次点解读直接打开。

- **雷达「解读」失败不再甩 Codex 整段日志**：后台一次性调用自动回落时跳过官方账号（OAuth 过期会 401），改走带 API 密钥的配置；失败只显示一句中文。快速开聊里你手选「官方账号」仍走 ChatGPT 登录——登录过期要到连接页重新登录，不是官方和 API 切错了。

- **编程页改成左工作树、右会话**：工作树卡不再把按钮甩到最右侧；进入 / 查看改动 / 拉取·推送分层，删除和合并收进 ⋯。git 状态改成芯片。项目路径可复制、可在文件夹里显示。本项目会话可收起，标题会清掉网址和中断提示，可搜索，悬停 ▶ 继续、保留、重命名、删除。

- **办公项目页改双栏**：左边文档、右边对话和定时任务。文件少于 4 个时不再单独出「继续上次」（和列表同一份文件），只在行上标「最近编辑」；相对路径只在有子目录时显示。表格用绿色图标，「进行中」改成带圆点的胶囊。没有对话时给出针对当前文件的建议和「发起新对话」，不再只写一句空状态。

- **Codex 走中转时不再带 ChatGPT 的优先档和网页搜索**：用网关跑 DeepSeek 等模型时，本进程关掉 `web_search`、把 `service_tier` 改成 `auto`（不改你的 `~/.codex/config.toml`）。官方账号启动不受影响。已打开的标签要重新启动才生效。

- **无流程项目页收口**：文献 / 笔记 / 数据 / 雷达 / 会话同一套列表（默认展开、先 10 条、可搜索）。只读文献身份段不放「写一句课题主题」（有研究流程的项目仍要，写进 TASK.md）。

- **项目里问 AI 先选 Agent / 配置 / 模型**：第一次弹出选择，可勾「设为默认」。默认落在终端（不是聊天层），右边打开这份文件（PDF 会撑开预览栏）。⌘ / Ctrl 再点可重选。官方账号不再把上次中转留下的 DeepSeek 模型注进去（否则 Codex 会报 service_tier `priority` 不支持该模型）。

- **工作台能跳到 Codex 客户端**：主卡底栏微字「在 Codex 打开」（不和「继续工作」抢层级），最近项目行「Codex」与「未添加 / 时间」同一套微字、排在左边。和对话页「在客户端打开」同一套深链，只在 macOS / Windows 显示。

- **技能「导入」弹层底栏收口**：浅色下不再用近黑按钮。「选择目录… / 选择 ZIP… / 导入」和「关闭」收成右下角一排（主操作在左、关闭在右），不再一个钮在左、关闭孤零零在右下。

- **办公「系统打开」能唤起 Excel 等本机应用**：不再走 WebView 的 `opener.open_path`（capability 默认不放行本地路径）。改由后端校验文件在项目根内、且是文档类扩展名，再用系统默认应用打开。

- **工作台最近项目能看出是不是你添加过的**：这一列来自各 Agent 会话扫到的仓库（外部 Codex 也会出现），不是项目页那份清单。已添加的显示你起的项目名；还没添加的右边标「未添加」，点进去仍是打开运行页。

- **项目页三处补口**：无流程科研文献行标「已读」（已有对应笔记）或「精读」（在精读清单、还没写笔记），点「笔记」打开这篇；办公文档可搜文件名或路径；编程工作树「查看改动」打开该树的终端改动面板（冲突行仍用「去解决」）。

- **MCP 页和设置页会跟着窗口变宽**：不再钉在约 1080px 居中。MCP 表格多出来的宽度给名称和配置列，开关仍在最右一列。设置里主题色卡、快捷键和定价表铺开，普通设置项的标签不会被拉得很宽。

- **PDF 预览改为连续滚动**：项目里点开、运行页右侧预览都上下滑，不再用顶栏 ‹ › 翻页。无流程科研页不再显示工作区（那是研究流程步骤用的隔离工作树）。

- **无流程页收口**：文献 / 笔记 / 数据 / 雷达默认折叠；PDF 和表格在当前窗口预览；文件夹里的 csv / xlsx / txt 进「数据」。编程页工作树结果会记住，切换不再卡在「读取工作树」。

- **四张项目页按同一骨架填密度**：无流程科研主区是文献、笔记、雷达（空的用一行式，不铺空卡）；流程科研只把冲突/待验收工作区提前，会话默认折叠。编程工作树带上次提交和改动数，冲突行置顶可去解决或取消合并，端口按本仓折叠。办公有「继续上次」、类型计数、对话徽标和定时巡检。问过的文件再问会把新问题发进原来的标签。

- **编程 / 办公页收口**：重复添加已登记或已有档案卡的项目不再把工作方式改回科研、也不再弹模板；办公「进行中」按文件是否在终端目录内判定（路径段边界，不会把 `ccode2` 算进 `ccode`）；合并/推送带超时并绕过 gpgsign；删分支 `-d` 失败会说明未合入提交，不再静默 `-D`；办公问 AI 按文件开标签；csv 走文本预览、大写扩展名进列表、老式 doc/rtf/幻灯请用系统应用打开；终端改动面板识别冲突文件并提供「取消合并」；工作台紧凑行带状态点与运行数，回落主卡也带工作方式；编程页可刷新，overview 按工作树/分支并行读 git；空仓库浏览会提示已写初始提交；编程 `git init` 先过 CLT stub 且用编程用 gitignore。

- **添加项目要先选工作方式**：科研、编程、办公三种，一个项目选一次。科研仍是课题主题 + 研究流程模板；编程按分支和工作树管并行改动；办公按文档库预览和问 AI。旧项目都算科研。

- **工作台能看见并行的几个项目**：有 Agent 在跑、或编程里要同步/合并、或科研还有未完成工作区时，会出现「正在进行」——第一条是大卡，其余是紧凑行。点继续：有对应标签就进运行页，否则进该项目自己的页面。

- **编程项目页**：主区是工作树状态（干净/领先落后/是否已 push，以及未开始、正在开发、需同步、等待合并、可清理）。可以创建/进入/删除工作树，分支台可以 fetch、推送、拉取、合并进基准分支、删除分支。新工作树放在 `~/ccode/worktrees/`。还不是 git 仓库时会先问一句再 `git init`。

- **办公项目页**：文档按最近打开排列，可用类型筛选。点开用和任务书一样的就地预览（md / pdf / Word / 表格 / 图片）；「问 AI」打开运行页的聊天层并带上文件。也可用系统应用打开，或在文件管理器里显示。

- **运行页标签栏再收口**：布局菜单拿掉。分屏对照和查找终端输出改成与聊天/终端、成果面板同一套描边图标；聊天层「窥视终端」改叫「拉起终端」，只留聊天头部这一处，菜单里不再重复。专注终端入口删除——它本来就是把左边工作树收起来，快速开聊也改成收起工作树（不再进专注态）。标签栏查找右边的 `⋯` 整颗去掉（停止走启动栏「停止」，恢复走空态「恢复任务」，接力/对话页走对话页）。聊天输入区左下角「插入 ⌄」换成加号图标。

- **只读文献不必挂研究流程**：沉浸阅读发现 PDF 还不在任何已添加项目里时，会问是否把所在文件夹添加为项目（默认不挂流程）。无研究流程时，「整理为笔记」写入项目根 `notes/inbox.md`，不再要求有精读步骤。添加项目后的模板层、以及项目页空步骤横幅，都写明：只读文献、写笔记，选「不使用研究流程」。

- **聊天里的代码和引用不那么抢眼**：行内 `代码` 去掉描边，只留浅底；引用块不再铺一整块灰底，只留左边一条线。笔记阅读版式不变。

- **Codex 官方账号走 ChatGPT 订阅**：启动栏选「官方账号」时，本进程用 `-c model_provider="openai"` 盖过磁盘 `config.toml` 里的网关默认（以前会继续走中转计费）。连接页「登录」改为 `codex login --device-auth`（终端里出现设备码，用浏览器打开授权页即可；不改你的配置文件）。未在启动栏选模型时，磁盘里的 `model` 仍可能生效。官方通道直连 OpenAI，国内网络常会 WebSocket/HTTPS 被重置——设置页新增「网络」：出网代理只注入官方账号启动和组头登录；网关启动不走；连接附加环境变量同名键可覆盖。

- **步进器滚动提示按溢出显示**：研究流程大圆底下「步骤较多，可左右滚动…」只在窗口真的排不下全部步骤时出现，宽屏不再常驻。

- **研究流程模板收口**：文献 PDF、清洗数据和渲染成品写到项目根（不进 git），合并时会把工作区里误写下的这些文件拷回主文件夹。毕业论文拆成「检索 → 精读 → 开题报告与综述」再往下走，和英文综述同名的文献步追加时会跳过。投稿能接毕业论文成稿，返修优先读已经按期刊改过的 `formatted.md`，格式适配会渲出 PDF。科研论文默认按计算实验写，接在综述后面时查漏补缺、不覆盖旧笔记。LaTeX 有成稿就转排版，没有才从笔记写。图以 SVG/PNG 进仓库。已有项目的步骤表不会自动改，重新选模板或追加才生效。

- **快速开聊收口**：随手聊历史只列 `~/ccode/scratch` 里的对话（Ccode 源码仓库这类未添加项目不再混进来）。弹层上半是新开聊，下半是「继续上次」（最近一条单独一钮，其余在下面）。侧栏点「快速开聊」记住过选择就直接开聊；想每次先选，勾「每次都先问我」。⌘K 和工作台页头仍打开弹层。历史打开即显示，回车即开聊。

- **工作台主卡跟真实运行对齐**：有 Agent 在跑时，「继续当前工作」显示那个标签所属的项目（已添加的用注册名，不显示文件夹名）和它自己的运行状态，点「继续工作」进运行页并激活该标签。别的仓库在跑不会写到这张卡上。没有运行时才用上次在项目页选中的项目，并带上流水线里第一个还没合并的步骤。待你处理空着时主卡拉满整行；最近项目按时间排；最近对话不再列出未命名条目。

- **中文输入法组词时按回车不再误发**：还没上屏就按 Enter 只确认候选词，不会把半成品发出去。

- **聊天层可以露出同一会话的终端**：聊天和终端还是同一个 Agent，点「拉起终端」会在聊天下方开出一块真正的终端（按这块高度重画，才能看见输入框和批准菜单）。只切聊天/终端时画面大小不变；打开或收起时终端会改高度。等待确认、登录卡住、或发出会打开选择器的命令时会自动露出；手动收起后这一轮不再自动打开。

- **聊天等待提示按阶段说话**：发送后显示「已送出 / 正在写盘 / 正在用某某工具」，而不再一律「正在处理」。工具调用一旦写入会话文件就会出现在聊天里；会话很长时可以「加载更早的消息」（压缩过的 Codex 会话没有更早一页，完整历史仍走「历史」）。

- **聊天里也能切模型、拖文件**：头部模型/思考档和底部状态栏走同一套命令；会打开方向键选择器的会先露出终端。文件拖进聊天输入框是一行一个路径。OpenCode 斜杠补了 `/models`，CodeBuddy 补了 `/login`。

- **对话页搜索能找正文**：搜索框会在标题/标签之外扫对话里说过的话。多个词用空格分开，先按相关程度（命中几个词、在标题还是正文），同分再按更新时间。命中正文的行下面带一句摘录；点开该对话会直接滚到那一段并标出来。第一次搜会建本机索引，可能稍慢。

- **对话页可导出会话包、换机导入**：列表「导入」选 `.ccode-sessions.zip`；多选或单条 ⋯「导出会话包」是原文拷贝（可能含密钥，导出前会说明）。导入向导按条显示能否落位，原目录在本机没有时要选目标文件夹；同 id 已存在的跳过、不覆盖。OpenCode 不能进包。Codex 用 Ccode 渠道的会话可勾选「注册到 Codex 客户端」，用本机连接补上另一台机器记下的渠道名。工作区里开的对话改写会话文件里的真实工作目录，不以列表归并后的主仓库路径为准。

- **运行页文件树能搜到 `.kimi-code` 一类隐藏配置**：搜索不再跳过所有点目录。`.git` / `node_modules` 等噪声仍不进；搜目录名、以 `.` 开头的查询、或打开「显示隐藏文件」后可以找到 `.kimi-code` 及其下的 config。点结果会自动打开隐藏文件显示以便定位。折叠箭头只负责展开/收起，连点不会再误进该文件夹。

- **运行页文件树按后缀搜**：`pdf`、`.pdf`、`*.docx` 按扩展名找（大小写不论）；`doc` 会带上 `.docx`。真正的 PDF/Word 排在文件名碰巧包含这些字母的结果前面。

- **运行页空白标签默认家目录**：第一次进运行、关掉最后一个标签、或点「＋」开的空标签，目录是家目录，不预填当前项目主仓，也不沿用上次开步的 `search` 等工作树。点进某个项目才切到该项目目录。上次没关就退出的任务标签仍按原目录恢复。

- **Excel 表格可预览**：文件树点 `.xlsx` / `.xls` / `.ods` 在右侧按工作表显示（列字母/行号冻结，宽表横滑，点格子看全文；最多 200 行 × 256 列）。Word 仍是 `.docx` 阅读版式；旧 `.doc` 会说明请另存为 docx。PDF 能力不变。

- **Markdown 阅读里的 PNG/GIF 不再裂图**：相对路径、`/Users/...` 这类绝对路径、中文文件名（marked 会百分号编码）以前被 WebView 当成网站地址去拉，出现问号裂图。现在先换成占位再按本地文件读取；https 图在文件预览里会显示（聊天里仍然不加载外链图）。文件树点开 png/jpg/gif/webp/svg 也内嵌预览，不再报「二进制不支持」。预览区重绘不再把已显示的图盖回「图片加载中」。

- **对话页范围不再把无头 AI 临时目录当项目**：`ccode-ai-<uuid>` 是 Ccode 自己生成摘要/提交信息时的临时工作目录，归入「Ccode 内部 AI」，不出现在「按项目」和 Agent 下的项目列表。

- **对话页范围 Agent 可折叠**：点 Agent 只展开/收起它下面的项目，不再被当前筛选钉死在展开态；筛这家仍点「全部项目」。

- **对话页范围按项目筛选**：范围选择面在 Agent 列表下方增加「按项目」分组（跨 Agent、按最近更新排序）。点项目只按目录筛选；点 Agent 仍可展开该 Agent 下的项目。

- **运行页标签栏收口**：聊天与终端合成一个单击切换按钮（图标显示当前层，再点切到另一层）。布局菜单去掉「隐藏/显示文件树」（左侧按钮已够用），成果面板升为标签栏右侧常驻按钮，与左侧文件树对仗。

- **连接登录 / 设置排版 / 折叠钮 / 工作树收起**：官方账号「登录」改成描边小按钮，不再是组头上孤零零的大字。设置里顶部导航入口改成可点芯片贴着排，快捷键名称和键帽挨在一起、三列铺开。全站分区折叠不再用小小的 ▸/▾ 字，换成统一的折叠模块。运行页工作树收起后整栏消失，打开按钮放在标签栏左侧（和项目页收起项目列表同一做法）。

- **设置快捷键铺满**：页面九个绑定三列格子，名称在左、键帽和禁用在右，不再两列拉开留白。

- **用量页三块洞察**：花费折线、费用数字和最贵 5 个会话都跟随页顶时间范围；本周/上周环比徽章（上周没用量、或当前范围盖不住上周就不出）；缓存命中率（有定价才写「若无缓存约」，即实付加缓存少付的合计，避免「省约」比费用还大）。官方账号订阅不计费、未计价模型不进花费榜。

- **用量页收口**：原先一串并列卡片收成两块——总览（费用/tokens + 花费·用量折线切换）和分布（最贵/Agent/项目/网关/任务同一组滑块，一次一张表）。模型表默认折起。折线与最贵榜同一时间范围、排除内部活动；自定义标题出站脱敏；最贵先排序再补标题。

- **主题成对校准**：沉浸黑 · 浅的选中/线条/正文改跟暖纸面，不再掺去蓝前的冷灰；mac 字色中间档同步转暖。Latte 浅改 Mauve 灰、灰蓝正红浅改冷石板，两套不再共用同一组底色；灰蓝正红浅的卡片层重新拉开。终端底/字色跟界面。陶土 / Ayu / 极简灰蓝 / Dracula 深浅搭配本身齐，未改。

- **设置 / 用量 / 连接 / 对话 / 勾选**：主题色卡改成该主题的小窗预览。用量主卡补输入/输出比例和场均，下面四格补密度。连接组头去掉版本号和刷新钮，官方入口收成「登录 / 官方」。对话范围改为整行选择面，打开时替换列表不挡住对话。原生黑勾选框改为主题 Checkbox（快速开聊、网关库、人工事项）。

- **设置分区芯片可辨**：每粒带描边与底色，选中用分段选中态，不再连成一条灰字。

- **连接页官方账号登录仍在组头**：未连接不再占一整行，但已安装的 Agent 组头保留「连接」入口（终端跑 CLI 登录），已连接显示「官方已连接」。

- **已删项目不再出文献雷达收件箱**：定时任务写在应用级 `schedules.json`，删项目以前只摘注册表，收件箱还会拿文件夹名提示「N 条新命中」。现在移除/清除痕迹/删除目录会清掉该根上的定时任务，列表出口丢掉孤儿，收件箱只对还在的项目出文献条目。

- **界面注意力收口**：顶栏待办收成一粒圆点（点开按类别列出，文献雷达不再占全局 chrome）。工作台「继续当前工作」优先已添加的研究项目，空收件箱不再占一张卡。项目身份行只留名称+课题，篇幅/读者/去向收成「全局设定」芯片；空雷达一行「还没订阅读」。运行页默认只留会话画布，文件树/成果面板按需滑出。连接页「+ 添加连接」只留页头，空组默认折叠，选用已有网关从目录勾选模型。技能「未用」不再写「0 个 Agent」；MCP 去掉导语。用量只强调总 tokens 与费用。设置分区改横向芯片。

- **连接页收口 + 网关体验走查修补**：行 ⋯ 只留日常动作（停用/默认/复制到其他 agent/设为全局/解绑），验证与启动计划预览降到分隔线下方；网关体检与清密钥进网关库；备份恢复进 Agent 组头。拉目录错误脱敏（gemini query 密钥不再进 UI）。漂移检测与设为全局同一套求交后再比对，托盘勾选改子集内核。导出 v2 剔除 extraEnv 密钥名、含密钥 0600、二次确认。导入按密钥指纹对不上时回落槽指纹，冲突进跳过明细。拆开自动合并记下原名并克隆网关。

- **网关库体验批次**：网关编辑按槽显示上次测速（✓ 延迟 / ✗ / 未测）与「全部测速」；模型行点开按求交三态编辑思考档/温度（绑了该网关的 Agent 通道取并集）；「获取模型」按 openai→anthropic→gemini→responses 顺序拉目录；配置页「全局生效」在外部手改后标黄；统计页新增「按网关」页签（只计经 Ccode 记下归属的会话）；导出网关+绑定 v2（可选含密钥）。

- **网关拆层走查修补**：恢复会话按 rollout 的 provider 现算注入名（旧 `ccode` / 新 `ccode-<gid>` 不再双向错配）；启动只取选中模型的温度/effort；迁移按 lastUsed 较新的策略赢、备份 keys.json、先回读钥匙串再分组；网关库补获取模型/按槽体检与逐模型策略编辑；添加连接可选用已有网关；托盘菜单随配置刷新、退出走关窗守卫、dry-run 外改提示对调；状态栏 `/effort` 未确认不显示。

- **网关 + 绑定拆层（配置模型）**：连接不再把 Agent、端点、密钥、模型、策略粘在一条 profile 上。端点+密钥进「网关」（按协议分槽），Agent 只绑定要用的模型名单；同一把中转密钥可绑多个 Agent。思考档/温度跟每个模型，Header 跟网关。终端状态栏只在「这个 Agent × 启动模型 × 网关」真能切思考档时才出滑条。升级时按密钥自动合并旧连接（binding id 不变，会话/定时任务不断链）。系统托盘可一键把某条绑定设为全局（只改外部终端用的文件，不改内嵌启动栏默认）。官方账号「设为全局」= 恢复该 CLI 初始配置。网关体检结果会落盘；从未体检不关掉控件，体检失败才关。详见 `docs/conventions/profiles.md`。

- **修复 Windows 上 agent CLI 无法安装**：此前 Windows 机器没装 Node.js 时 codex/claude 等没有任何可用安装渠道（brew 仅 macOS）；新增 winget 原生渠道——claude/codex/opencode/kimi/grok 五家有官方包（Anthropic.ClaudeCode / OpenAI.Codex / SST.opencode / MoonshotAI.KimiCodeCLI / xAI.GrokBuild，portable 免管理员），装完立即可检测、可更新（`winget upgrade`）、可查新版；claude 顺带补了 npm 渠道（`@anthropic-ai/claude-code`）。同时修复 Windows 上经 PTY 执行 npm 系 .cmd shim 必挂的两个叠加问题：候选目录里同名 shell 脚本抢在 .cmd 前被选中（os error 193），以及 ConPTY 里 npm 发终端光标查询无人应答导致永久挂起（安装器现在会代答）；shim 统一解析出 JS 入口改用 node 直启（安装/更新、版本探测与终端拉起 agent 同口径），npm 安装/更新附加明确网络参数（30 秒请求超时、不重试），registry 不可达时快速报错而不是长时间无输出；另修复 grok 精确注意力开关在 Windows 上二次切换被误判「外来文件」拒绝（JSON 转义的 `\\` 路径分隔符归一化漏按对处理）

- **外部技能流水线适配五件套**：GitHub 下载的科研技能现在即装即配——① TASK.md「本步骤技能」段固定带「路径冲突以本文件为准」兜底句，未适配技能也会把产物写进约定落点；② 技能 frontmatter 新增 `inputs`/`outputs` 接口声明解析，外部技能没声明时自动从正文推断（标注「推断」）；③ 步骤技能区新增链路校验 ⚠：技能要读的文件上游没人产出、或产出不在本步骤预期产物里，挂载时即提示（开工确认弹层与研究流程编辑器都有）；④ 技能页「⋯ → ◈ 适配到流水线」：AI 按流水线路径约定改写技能并补写接口声明，预览确认后才写回（有备份）；⑤ 技能页「⋯ → 挂载到步骤」/ 详情面板「＋ 挂载到步骤」：选项目选步骤一步挂进研究流程

- **「草稿」概念并入 TASK.md**：界面上没有「草稿」了——流程线「预览 TASK.md」与「预览/编辑草稿」合并为一个「预览/编辑 TASK.md」：没编辑过看到的是模板默认拼装，可直接改，保存后开工就以这份为准；「跟 AI 商量一下」开聊时若还没编辑过，先把模板拼装（简报/预期产物/提货单）整份写入作为起点，Agent 先通读、把拿不准的点逐个问你、按回答直接改这份文件——商量的结果就是最终落盘的 TASK.md，不再从零起草丢掉模板内容；「确定文献来源」的切换会就地同步进已编辑过的 TASK.md（「文献来源」段跟着配置走，不会停在旧快照）
- **「先定几件事」挑明两条路径**：确定 TASK.md 分方式一/方式二——方式一「点卡片直接定」（展开点选项即答，进「已定方向」），方式二「聊着定」（展开点「跟 AI 商量一下」，AI 读稿提问后直接改 TASK.md）；两条路同形（都是默认折叠的一行）、终点相同、都不做也能开工。没有决策卡片的步骤不标「方式二」，聊天入口为单按钮（可选 · 边聊边改 TASK.md）
- **无启动注入的 CLI（Kimi/OpenCode）指令自动复制**：原先启动后指令文本留在不可选中的输入框里，「请手动发送」无从下手；现在自动复制到剪贴板并提示，指令行旁另有「⧉」一键复制兜底

**补齐（走查后追加）**

- **界面不再出现「注册」这个内部术语**：统一成「添加 ↔ 移除」——添加项目弹层的按钮从「注册」改「添加」，状态「未注册」改「未添加」，菜单「移除项目注册」改「从 Ccode 移除」（也更好地和「删除项目目录」区分开）
- 工作区空页面去重：页头那句宣传语删掉（下方空状态已经说过一遍），说明从两行压到一行，「创建示例课题（演示）」去掉冗余的括号
- 模板选择页：五套模板的说明去掉「步骤链 + 一句空洞总结」里的后半句（链条本身就是最好的说明，且总结会导致折行）；底部两个出口原先都叫「不选」看不出区别，改为「不用，别再提示」与「稍后再选」
**修复：流程顺序与孤立按钮**

- 「确定文献来源」在科研论文/毕业论文模板里排到了「先定几件事」后面——文献从哪来是整步的输入前提，连讨论都该在知道来源之后进行。已提前

- **「预览 TASK.md / 本步骤的对话」不见了**：上一轮调整顺序时把这段代码错放进了「常见问题」的折叠容器里，只有展开那一档才渲染。已复位
- 「聊天时不让 AI 改文件」开关改为跟随想法区一起出现——没聊过话题时它没有约束对象
- 当前步骤的蓝色竖条不再向上溢出（原先撑满整行高，比序号圈高出一截）

- **当前步骤的蓝色竖线终于对齐了**：它原本画在整行最左边，而序号和连接线在往右 7px 处，两条不同的竖线。现在竖线压在序号那一列上，视觉上就是「框住这个序号和它下方的线」
- 「点「开始」交给 AI」删掉——右边就是「开始」按钮，等于指着按钮说「点这个按钮」
- **「跟 AI 商量一下」移到「预览 TASK.md」上方**：它改的就是 TASK.md，理应排在预览前面。同时标上「可选」——它和「开始」是并列的两条路，不是开工前必经的一步
- 右下角那个孤零零的开关，标签改为「聊天时不让 AI 改文件」（说清它约束的是哪件事），并改为悬停才显示

- 流程线四处去重：删掉「← 当前」标签（序号已经用强调色标出当前，左侧还有竖线和最亮的文字，这是第四个重复信号）；「文献从哪来」标签删掉（节点名就叫「确定文献来源」）；选项下方那句说明删掉（和选项自己的悬浮提示重复），导入链接并进同一行；文献来源那一块的左缘对齐到步骤名，不再顶到序号上

- **项目设置里的「全局设定」只看得见一行**：输入框复用了单行样式（写死 28px 高），把 4 行内容压成 1 行——数据其实都在，只是看不见。现在是 4 行高、可拖拽调整
- **改完没有任何反馈**：全局设定是失焦自动保存的，但存完悄无声息。现在保存成功会显示「✓ 已保存」（2 秒消退），并补一句格式说明「一行一条，写成「问题：答案」；每一步开工时都会带给 AI」

- **人工事项勾不掉**：取消勾选后会立刻自动勾回来。两个原因——① 上一轮给「补充你已知的关键文献」加的放宽规则（声明了文献库且登记过 PDF 就算完成）没跟着那条事项一起删掉，导致「下载付费墙文献全文」被无条件判定为已完成；② 取消勾选原本是「删除记录、回落到文件检测」，只要落点里有文件就会再次自动打勾。现在取消会明确记下来，检测命中也不再覆盖你的选择

- **人工事项行不再出现两个勾**：复选框本身就既是状态又是控件，左边再画一个 ✓ 是同一件事说两遍
- 「可选」小标签不再和「可选（不做也能跑）」分组标题重复显示

- **「确定文献来源」现在排在「AI 干活」前面**：原先它挂在 AI 那一格的内容区里，「开始」按钮显示在它上方——等于先出发再问路。现在自成一格，没交代来源时它就是当前步骤
- 顶部状态语「待开始 · 先定下方几件事」删掉——下方那几件事已经移走了，这句话指向一个不存在的东西
- **项目头不再显示未填写的设定**：模板预填的是「综述角度：（领域全景 / 聚焦某个子问题）」这种空答案，原样显示成了一串没意义的问号，还容易被当成已定的结论
- **说清那两个按钮是干嘛的**：「跟 AI 商量一下」旁补一句「拿不准这一步要做什么，就先聊聊；结论会写成任务书」；「＋ 话题」改为只在已聊过话题时出现（它是给已有讨论起分支用的）

**研究流程重新设计（专家 / 新手双视角，五套模板全量）**

- **每一步标出「轮到谁」**：AI 做 / 你来 / 一起。步骤名保留学术术语，角色单独标记——扫一眼就知道哪几步要自己出场。流程感来自角色交替，不是序号
- **贯穿全程的设定提到项目层**：综述角度、目标篇幅、读者与文风、投稿去向这类决定后面每一步的事，从「第 1 步的折叠区」移到项目头，一次定好、每步的 TASK.md 都带上。选模板时会预填几个空位让你补
- **「先定几件事」这一格没东西要定时不再出现**：整篇级设定上移、要看数据的问题改按需问之后，检索这类步骤的开工前问题归零，那一格只剩一个按钮，空着还让人以为漏了什么。现在流程线直接从「AI 干活」开始。想法区和「跟 AI 商量」挪到了 AI 那一格下面，功能没少
- **五套模板逐一复盘**（不只综述）：科研论文的「课题边界 / 研究问题 / 目标期刊」、毕业论文的「语种 / 查重目标」、投稿的「目标期刊」都提到了项目层；数据处理的「缺失值怎么处理 / 分析粒度 / 要不要合表」改为 AI 先报缺失率与表间关系再问——这三件事不看数据根本答不了；科研论文的「算力怎么排」改为先试跑估算耗时再问
- 顺带修正四处角色标注：「结果分析」「研究方法」「探索性分析」由「AI 做」改「一起」（结论解读和路线选择是人的判断），「润色与投稿准备」改「你来」（投稿材料要人签字）
- **需要看到数据才能定的问题，改为按需问**：「纳入标准定多严」「检索范围铺多广」原先摆在开工前——但标准松紧取决于命中量，开工前问等于让你在没有数字的时候猜。现在 AI 先粗检报数（「命中约 2800 篇，建议标准如下」）再问你，不回复就按它的建议继续，不停工

**步骤流程：让「开始」成为唯一主路径**

- **「开始」按钮做大一档**，并明说「上面那些不定也行——点「开始」，AI 会在对话里问你」。实测发现那几道题**从来就没拦着开工**，但摆成一列待答清单看着像必办任务
- **常见问题默认折叠**：标题从「还有 3 件要定」改为「3 个常见问题，点一下就答完」。想省事就展开点两下推荐值，不想答直接开工
- **两个聊天入口合并成一个**「跟 AI 商量一下」（结论自动写进任务书草稿）。原先「跟 Agent 聊任务书」与「聊个话题」的区别对新用户没有意义——两个都是"去聊"。预置话题降为「或直接聊：」的现成话头，「话题」区退化为已聊过话题的归档清单，没聊过时不显示

**步骤流程页：术语、流程感、字号**

- **节点带序号并串成一条线**：①②③ 依次是「先定几件事 → AI 干活 → 你验收」，节点之间画竖连接线。原先三个 ○ 平铺，看不出先后，也看不出这是一条流程
- **术语换成白话**：「定方向」→「先定几件事」；「这一步怎么走」→「这一步的流程」；「agent 执行」→「AI 干活」；「评审合并进主文件夹」→「你验收，合并进主文件夹」；「只读保护」→「不让 AI 改文件」；「建议先点下方种子聊聊」→「先定下方几件事」
- **字号统一**：同一块里原先 micro/xs/sm 三档混排。规则改为——可点的东西（按钮、选项）一律同档，只有真·脚注保持最小号

**修复**

- **两条横幅自相矛盾**：顶部说「项目目录里没扫到 PDF / 数据 / 引文」，项目里却显示「已自动扫描：1 个引文 · 10 个论文」。原因是顶部那句只数「本次新登记的」，重复添加已有项目时是 0——改为按项目当前实际拥有的资源数说话
- **刚选完模板就报「追加 0 个研究步骤；跳过 5 步（同名）」**：项目本来就有这套流程时（多为重复添加），这句既像出错又没信息量，改为「项目已有「英文综述」的全部步骤，没有重复添加」

- **界面小字大幅精简**：一个操作最多配一句话，说不完的收进悬浮提示。砍掉的都是「解释这东西为什么这么设计」的段落——那是文档该干的事，不该占界面版面。全站最长的界面说明从 122 字压到 42 字，超长段落（≥45 字）清零

- **顶栏撤掉中间的搜索框**：顶栏摆输入框不好看。命令面板入口仍在侧栏底部 + ⌘K，不另设第二处
- **对话行操作分级**：⚑ 保留提为行内（已保留时状态标记本身可点取消），⋯ 菜单从 11 项平铺改为三组——整理 / 继续 / 危险；「在外部终端恢复」与「复制恢复命令」并为「在外部继续 · 打开终端 / 复制命令」
- **技能详情新增「哪些步骤在用」**：反查全部已注册项目的研究步骤，点击直达该项目。删技能前终于知道会影响谁
- **MCP 分发状态三态点**：已写入（绿）/ 被外部改过（黄）/ 未开启（无点）。「被外部改过」的能力后端一直有，但此前只在删除时用来拦截，界面上看不到——你不知道自己在 agent 侧手改过的东西下次保存会被覆盖
- **设置页新增「数据与存储」**：列出 app.db / 配置 / 技能库 / 会话快照 / 备份 / 模型缓存各自体积与路径，可一键在文件管理器打开
- 设置页补上「想法期只读保护」开关（原先只能从工作区卡片区找到）

**收件箱与顶栏（批次 6）**

- **顶部那条栏不再空着**：左边显示「当前项目 · 步骤」（点击回工作区页），中间是「搜索与命令 ⌘K」入口，右边是运行中的 agent 数与收件箱胶囊。macOS 上这条栏本来就恒占 40px（红绿灯要让位），此前只在有待办时才有内容。执行态（⌘\）下自动只留收件箱
- ⌘K 从侧栏底部那个几乎没人发现的小字入口，提到了顶栏中间
- **收件箱每一条都能忽略了**（原先只有「人工请求」能）：忽略后状态一变会自动重新出现，所以忽略不等于漏掉

**对话页筛选（批次 4）**

- **一行 chip 快筛**：⚑ 保留 / 进行中 / 今天 / 近 7 天 / 内部 AI / 已归档。这些维度的数据早就在会话记录里，以前一个都没进筛选界面——而唯一做进界面的 agent 维度恰恰最低频（你是按项目、按时间找，不是按 agent 找）
- **搜索框给结构化建议**：输入「检索」会列出「步骤：文献检索与筛选」「卡片：检索策略」等，点一下落成可叠加的筛选 chip，取代原先「展开手风琴 → 找 agent → 展开 → 找项目」的三次点击
- **对话与工作区打通**：步骤流程线新增「本步骤的对话」直达；对话行的 ⎇ 步骤/工作区标记从只能看变成可点，点它回工作区页并选中该项目

**终端工作台减负（批次 3）**

- **标签条从 13 类控件降到 5 类**：分屏 / 工作台 / 专注终端三个开关合并成一个分段控件（它们本来就是同一件事——这块屏怎么排）；git 芯片、「✓ 保存」、「可合并」状态下移到终端下方的状态条——它们是结果不是入口，放顶上跟标签抢注意力。保存和查看改动的行为一字没变
- 「⋯」终端操作菜单改为常驻（原先只在专注模式才出现，非专注时同一批动作散在启动栏的另一个「⋯」里）
- 右侧面板的「完整回放」「专注内容」「收起」改为悬停才显示

**配置与统计（批次 5）**

- **配置行「⋯ → 隐藏此配置」**：隐藏后仍可正常使用，只是在终端启动栏的配置下拉里沉到「更多（已隐藏）」分组，自动预选也会跳过它。这是「停用」的准确形态——注入模式下配置只在启动那一刻生效，没被选中的配置本来就不产生作用，所以不做会误导人的 enabled 开关
- **gemini / cursor 不再显示空的「从预设快速填充」下拉**，改为直接说明为什么没有预设（gemini 用官方账号时 Base URL 留空即可；cursor 端点是自家协议，第三方端点接不通）
- grok 补上 OpenRouter / DeepSeek / 智谱等 OpenAI 兼容端点预设

- **修好「配置了却切不动模型」**，四个成因逐条修：① 换配置时手填的模型不再被静默清掉（不在新配置列表里就保留，并说明「仍按原样注入」）；② 配置没填模型时明说「启动不注入模型参数，CLI 用自己的默认值」——这以前是个纯静默的坑；③ 各 CLI 的多模型能力差异（gemini/qwen/kimi/grok 只认一个、codex 要重开标签、cursor 是启动参数）直接显示在启动栏模型框下方，不再只写在配置页表单和手册里；④ 手填的模型串不像 id 时给黄色软提示（不拦截——中转厂商的模型名千奇百怪）
- **保存配置后，若该 agent 有标签在跑，提示「改动要重开标签才生效」**：配置是在启动那一刻注入子进程的，这条规则以前只在手册里
- **每个 agent 可设默认配置**：配置行「⋯ → 设为该 agent 默认」，终端启动栏选完 agent 自动预选它（解析顺序：默认 > 上次使用 > 首个）。这就是「启用」的准确形态——注入模式没有全局激活态，所以不做 enabled 开关
- **配置行新增「⌨ 在终端使用」**：直接用这套配置开一个终端标签。以前建完配置没有任何通往终端的路
- **统计页新增每日用量趋势线**：区间总数看不出「这周比上周多花多少」。手绘 SVG，不引图表库
- **技能页新增搜索**（名称 / 描述 / 分类 / 来源）
- 设置页新增「启动行为」（启动时进入哪一页）与「关于」（版本 / 项目主页）两个分区

**快速开聊落到干净终端**

- 从「快速开聊」进终端时自动收起工作树与右侧面板，只留标签条和终端——随手聊没有项目上下文，文件树是空的、改动面板是「不是 git 仓库」，摆着只是噪音。Esc 或 ⤢ 随时退出

**研究流程：开工前的「输入准备」独立成块**

- **「文献从哪来」提到流程线「定方向」里**，三选一：让 agent 检索 / 我有 Zotero 库 / 我有一堆 PDF·题录；旁边的说明与**动作链接都跟着你选的走**——「去补几篇 →」/「去导入 Zotero 库 →」/「去放入题录 / PDF →」，点了落到「文献与数据」并高亮该用的那个入口（三个选项配同一句「去导入」的话，选择就白选了）。这个问题原先在四个地方问你（设置抽屉的开关、Zotero 导入按钮、决策项、人工事项），彼此还不通气
- **它和上面的「决策项」刻意做成两类**：决策项的答案写进任务书草稿、只是记录；「文献从哪来」的答案写进项目配置、要动手导入、还会改变这一步的性质（系统检索 → 盘点已有 + 查漏补缺）。所以单独成块
- **分工定死**：「输入准备」管**从哪来**，「文献与数据」管**有什么 + 进料**。导入动作只有「文献与数据」一处，不在流程线复制第二套按钮
- **删掉冗余的人工事项「补充你已知的关键文献」**：它和「输入准备」完全重合，而且它的落点写着 `papers/`——从 Zotero 导入的文献留在原处只登记位置、永远不会进 `papers/`，这个勾等于永远打不上。（只影响新建项目；已有项目里的那条需要自己到流程编辑器里删）
- **流程编辑器里说清「改了会不会显示在流程上」**：展开「人机分工」和「高级」时，区首直接写明——人工事项/决策项/讨论种子都会出现在流程线上；工作区名、run 脚本、资源绑定是幕后生效的（并提醒：已建过工作区后改工作区名会让这一步失去绑定）
- 哪一步要问文献来源由模板声明（`asks_lit_source`），引擎不去猜；流程编辑器「高级」里可开关

**修复**

- **修好「按提示导入了、系统却不认账」**：落点写 `papers/` 的人工事项，在你已声明「我有 Zotero 库 / 我有一堆 PDF」之后，只要项目里登记了 PDF 资源就算完成——不必再把文献往 `papers/` 里另放一份。选「让 agent 检索」时口径完全不变
- 上一条的连带修正：示例课题里还藏着一份重复的「补充你已知的关键文献」（而且是必办），一并删除
- 新建项目后「已自动扫描到 N 个文献/数据」的反馈看不见了——上一批把「文献与数据」面板搬进设置抽屉后，新项目的自动展开变成「在关着的抽屉里展开」。现在扫描结果直接显示在项目详情页的新项目提示条上，并带「查看 / 补充」直达

**界面优化第 2 批：工作区与项目管理**

**界面优化第 2 批：工作区与项目管理**

- **新增「快速开聊」**：侧栏「工作」组顶部与 ⌘K 命令面板各一个入口，选完 Agent 和配置直接开一个终端标签就能聊——不建项目、不建工作区、不写任何配置文件。默认落脚 `~/ccode/scratch`。聊出东西了从终端标签「⋯ → 转为项目」把这个目录登记成项目，之前的对话自动归到新项目下
- **项目详情页新增「当前步骤卡」**：步骤名、白话状态和操作流程线合并成一张卡，一眼就知道「现在该干嘛」——原先这三样分散在三行里。研究流程步进器保持原样不变
- 项目名放大一档；没写课题主题时直接给一个可点的「＋ 写一句课题主题」，不用再去菜单里找
- **研究流程编辑器：一屏从放不下一张步骤卡变成放得下三张**。常驻只留步骤名、简报、预期产物、推荐技能，其余按「人机分工」（人工事项/决策项/讨论种子）与「高级」（工作区名/run 脚本/资源绑定）折叠。字段一个没删，折叠区标题会显示「已填 N 项」，填过的默认展开
- 推荐技能现在也能在流程编辑器里改（原先只能在开工弹层里）
- **新增「项目设置」抽屉**：项目名、课题主题、研究流程模板、文献与数据、定时巡检收在一处。项目「⋯」菜单从八项收到四项，详情页少两条常驻区块
- **步骤「⋯」菜单从六项精简到一到三项**：删掉「人工事项」（点它和点步骤大圆完全一样）、「复制工作区名」、「删除步骤」（改到流程编辑器里删，破坏性动作不放在悬停菜单）；「产物核验 / 定位目录」在还没有工作区时直接不显示，不再灰着占位。菜单里现在没有任何破坏性动作
- 流程编辑器的两个折叠区改为默认收起（原先模板步骤个个都填了字段，导致两档全是展开的、等于没折叠），标题改显示真实明细如「人工事项 2 · 决策项 3」

**修复**

- 研究流程编辑器保存时会把人工事项的「可选」标记抹掉，导致内置模板里标着「不做也能跑」的事项被静默升级为必办；同时补上了「可选」勾选框

**界面优化第 1 批：浅色补完与去线条化**

- **修复浅色主题下终端字看不清**：四套 ANSI 调色板原本全是深色向的，直接铺在浅色主题的近白底上——「白色」`#e5e5e5` 对比度约 1.05:1 等于隐形、「亮白」`#ffffff` 完全看不见、黄色约 1.4:1 读不了，光标几乎不可见，选中文字被深藏蓝选区压成不可读。新增四套配对浅色调色板（Light+ / Solarized Light / One Light / Latte），光标与选区颜色一并随调色板走；切主题时自动换到配对的那套，设置页只列出与当前主题明暗匹配的四套
- 顺带修掉 Catppuccin 浅色主题「界面用 Latte、终端仍是 Mocha 深色」的不一致
- **浅色主题状态色补齐**：可合并/冲突/待确认等状态标签在浅色下原本是白页上的深绿/深红/深黄色块（全站 69 处），改为浅底深字；diff 的 +N/-M 行数颜色同批修正
- 修复三处状态圆点在浅色主题下几乎消失：研究流程步进器的「阻塞」大圆、大圆右上角的待确认角标、任务卡区的「主仓改动未存入历史」圆点
- 浅色下弹窗/下拉与页面底色糊在一起（白底上看不见白色高光边），改为可见的边界线
- **去线条化**：工作区列表从一串细行分隔线改为独立卡片 + 卡间留白；项目名称行、端口折叠区标题的横线拆除；任务卡、想法卡、定时任务三处行分隔线改为留白 + 悬停高亮
- **修复浅色主题下开关（Toggle）显示成黑疙瘩**：滑块原本用「文字最亮档」颜色，浅色主题下那是近黑色，配上近白的轨道后明暗全反；现在滑块恒为白色、状态由轨道颜色表达
- **浅色主题整体发平的问题**：七套浅色的「页面底 → 面板 → 卡片 → 浮层」四层明暗差原本只有 1–4（几乎看不出层次），卡片和页面底糊在一起；统一重排为每层差约 4.5，页面底稍压深换出层次空间，各主题色相不变
- 新增主题令牌自检测试：明暗层次、浮起次序、选中/线条深度、状态色对比度（WCAG AA）、开关明暗，改主题色时会直接报错

**研究流程简化**

- 讨论种子拆分为**决策项**（`[[steps.decisions]]`，问题 + 可枚举选项）与开放题：可枚举的在流程线上点选即答，不开终端、不建会话、不切页；新增「全部用推荐值」一键应用默认值（只填未答的，不覆盖已选）。内置五套模板共 30 个决策项 + 14 个开放题
- 决策项答案写进任务书草稿的「已定方向」小节，并作为 `## 已定方向` 段进入 TASK.md（排在简报之前，注明冲突时以其为准）
- 讨论入口从四条收敛为两条：「跟 Agent 聊任务书」（可写，agent 直接改草稿）与「聊个话题 / 种子」（只读讨论，聊完「◈ 沉淀进任务书」）。删除「＋ 自定义话题」（与前者重复）
- 想法区收进流程线的「定方向」节点内，不再是并列区块；discuss 节点恒存在（想法的落点）
- 开工弹层的 TASK.md 编辑区默认折叠为一行；草稿支持就地预览/编辑（不再跳终端页），TASK.md 与草稿均可渲染 markdown
- 两个「（可选）配置 MCP」人工事项从模板移除（一次性应用级配置不该每步问一遍），信息并入「补充你已知的关键文献」的说明

**新增**

- **Zotero 进料口**（只读适配器，不做文献管理器）：读本机 Zotero 库 → 生成 `references.bib`，已下载 PDF 按绝对路径登记为项目资源（只记位置不复制）；`lit_source` 字段让检索步骤降级为「盘点 + 查漏补缺」。库文件复制副本后只读打开，绝不写回
- 「只读保护」如实标注当前 agent 是否有进程级硬保护（qwen / opencode 无只读启动参数，仅提示约束）

**修复**

- 人工事项拖拽提交在「开始前」「进行中」两组静默失效（ref 挂在分组内的 `<ul>` 上，只有最后一组生效）
- 「◈ 沉淀进任务书」由整份覆盖草稿改为**追加**，不再可能删掉已有内容
- 「已定方向」小节的边界判定改为「只由空行与答案行组成」，避免重写小节时吞掉紧随其后的正文
- 只点选项生成的草稿不再顶掉模板拼装（否则 TASK.md 会只剩几行、丢掉整份简报）
- 聚焦步骤改用步骤名做主键：流水线编辑器里重排/改名后不再静默跳到另一个步骤
- `sessions.rs` 打开 `app.db` 补 `busy_timeout`（与另两个连接同口径，消除并发写下的 `SQLITE_BUSY` 缺口）
- 导入的检索题录落项目根时提供「登记为项目资源」，并修正简报措辞——原文让 agent 查工作区内的 `papers/imports/`，而该目录在工作树里不存在
- README 的 agent 数量、页切快捷键、主题数已过期；`figure-forge` 技能引用了不存在的 `figure-designer`

## v0.1.0（2026-08-08，首个发布版本）

**科研流水线**

- 项目注册与 `.ccode/project.toml` 档案卡：工作区页按项目分组，组顶部为流水线大圆步进器概览（方块节律虚线贯穿 + 纯色状态大圆 + 圆前/圆后小方块（混在虚线节律里、hover 才现可点）= 编辑简报/产物入口 + ⋯），点大圆按状态推进步骤（开步/恢复/跳终端），状态从绑定工作区自动派生，标题行显示「研究流程 d/t」计数与「⚠ N 条提示」校验徽标；未注册目录组头「注册项目」与顶部「+ 添加项目」走同一注册弹窗
- 一键开步：点未开始步骤的大圆 = 非 git 目录引导 init（自动生成含 `*.pdf` 的 .gitignore）→ 自动把 `.ccode` 与 `.gitignore` 提交进主仓库（仅这两个自有路径）→ 建绑定工作区 → 简报落成 `TASK.md`（自动加入 `.git/info/exclude`，不随合并污染主项目）→ 终端预填启动；课题主题可编辑并写进 TASK.md
- 流水线模板库：内置四套（英文综述/科研论文/数据处理/毕业论文）+ 当前流水线「另存为模板」（用户模板可删除）；「重置为模板」替换步骤不影响已绑定工作区
- 「编辑流水线」全宽编辑器是步骤编辑唯一入口：每步一张卡片（步骤名/工作区名/简报/预期产物/run 脚本/资源绑定/推荐技能），↑↓ 排序、整体写回；资源绑定默认空 = 全部资源，勾选后 TASK.md「项目资源」段只列选中项
- 资源面板：登记 PDF/CSV/parquet/bib 等资料（只记路径不复制），「发现资源」自动扫描项目目录列候选一键登记，资源行可复制绝对路径粘贴给 agent；PDF 资源直接内嵌「查看」
- 步骤产物核验面板（任务行「产物」与步进器圆后小方块两处同一面板）：进行中/待评审列工作树下的预期产物，已完成后列主文件夹同名产物，点击文件跳终端页预览；步骤「⋯」内「◫ 定位目录」把终端文件树根切到该工作区
- 提货单 `artifacts.yaml`：改动面板登记大产物（md5 + 大小，已 git 跟踪的文件拒绝登记），清单随分支提交；下一步开步时项目根提货单自动写进 TASK.md「上一步产物」段
- 论文类模板自带 quarto 脚本：开步写入 `.ccode/settings.toml`，工作区行「运行脚本」出现 `render-draft` / `render-final` 渲染 PDF 与 `export-docx` 导出 docx
- 评审合并成功「▶ 开始下一步」：合并成功且保留工作区时，成功横幅给出「▶ 开始下一步：步骤名」（下一步 = 同名步骤之后第一个无同名工作区、含已归档的步骤）；无下一步只显示合并成功横幅，「合并并归档」不出此入口；开步 invoke 链路单一出处 `pipeline-start.ts`，步进器大圆与评审入口共用

**阅读与写作**

- PDF 内嵌预览（pdf.js，懒加载分包）：页码导航/跳页/缩放，只渲染当前页与相邻页；拖选文字浮出「◈ 问 AI」把选段 + 出处（文件名、页码）写入活跃终端输入框（不自动发送）；浮动条另有「↵ 直接发送」——选段 + 出处一次拼接回车注入，同帧到达防半截输入
- 「整理为笔记」：PDF 选段（含出处）追加到归属项目「文献精读与笔记」工作区的 `notes/inbox.md`；没有该工作区时自动一键开步创建并预填终端启动；未登记资源的 PDF 提示去工作区页登记
- Markdown 阅读版式：md 预览默认渲染排版（标题/引用/表格/代码块，配色随主题），「阅读/编辑」切换不丢未保存改动；「⛶ 沉浸阅读/沉浸编辑」全宽覆盖层（Esc 退出，终端保持运行）
- 笔记对话式批改：md 阅读版式（含沉浸）拖选文字浮出「◈ 讨论/改写此段」，选段 + 出处写入活跃终端 agent 输入框，接着输入你的意见后自行发送
- docx 阅读版式预览：mammoth 转排版视图（图片内嵌，配色随主题），只读；超过 50MB 明确提示不渲染
- 「◈ 接力到…」：从当前会话尾部生成结构化接力简报（脱敏后落 `.ccode/handoff-<时间>.md`），新终端标签带简报启动目标 Agent；Kimi/OpenCode 无注入参数走复制简报路径手动发送；接力会话在对话页显示「⇄ 接自 <Agent>」，接力链可回溯

**评审与验收**

- 逐 hunk 验收：改动面板展开的单文件 diff 中每个改动块可单独「丢弃」（`git apply -R`，二次确认）或「暂存」（`git apply --cached`）；勾选提交遇部分暂存走临时索引提交，只带走已暂存的块；新文件整个算一块（暂存=跟踪，丢弃=删文件）；仅覆盖未提交改动，已提交累计 diff 仍走评审
- 评审内快速提交：合并被「主文件夹里还有没保存的改动」拦截时，可在评审覆盖层内直接列出主仓文件勾选提交（默认不勾选、信息留空走本地默认、不调 AI），成功自动重新健康检查
- 图片双栏看图：评审与改动面板中 png/jpg/gif/webp/svg/bmp 直接「基准 vs 当前」对照（透明图带棋盘格底），单图超 20MB 才回落提示

**监督**

- 长任务 OS 通知：agent 从工作中转为待确认/已完成且窗口未聚焦时发系统通知（同标签 30 秒去抖，设置页可关）；通知带「去处理」动作——已完成且 cwd 命中工作区直达评审覆盖层，待确认聚焦对应终端标签
- 首页「待你处理」收件箱：聚合工作区冲突/可合并 + 终端待确认/已完成，排序 冲突 > 待确认 > 可合并 > 已完成，为空整块不渲染；横跨项目导航与详情两栏之上，跳终端激活标签走一次性请求（已关闭标签静默忽略）；终端运行状态经镜像跨页只读（不新增轮询），外部 live 会话由后端 `session_tail_state` 直查；原左栏「运行中」折叠面板移除
- 端口监控：LISTEN 端口列表区分「本应用 / 系统其他」两段（cwd 最长前缀归属，回落 `CCODE_PORT` 段标注），终止系统其他进程必须二次确认
- 任务成本：统计页按工作区/任务归因 tokens 与费用（usage schema 升 v5 自动重建索引）
- 订阅口径：官方账号（如 Google 登录）启动产生的用量显示「订阅」不按量计费，token 数仍实际统计（schema 升 v6）
- 对话页新增「历史时间线视图」

**技能**

- 新建/编辑技能：「+ 新建技能」与行内「⋯ → 编辑内容」内联编辑名称/描述/正文（frontmatter 自动生成），重名拒绝并引导改用编辑，保存前自动备份（保留最近 5 份）
- 「◈ 优化」：写下优化意见后开终端标签，Agent 阅读并直接改写库中的 SKILL.md，备份兜底靠既有保存/覆盖路径
- 选段「✦ 沉淀为技能」：md/PDF 选段 → AI 提炼 SKILL.md 草稿（脱敏 + 8KB 截断 + JSON 容错解析）→ 技能页新建弹窗预填，保存走既有新建路径（重名拒绝）
- 技能列表增强：用户自定义标签（名称后 1–4 个 pill）、GitHub 导入自动分类、来源 owner/repo 显示为可点链接、详情「◈ 翻译为中文」
- 步骤推荐技能：步骤可挂 `skills = [...]`，一键开步的 TASK.md 落成「本步骤推荐技能」段（未安装的标注）
- 科研技能包：`lit-search` / `lit-notes` / `review-framework` / `review-writing`（综述）、`data-clean` / `data-eda`（数据）、`quarto-render`（渲染排错）

**配置与账号**

- 官方账号 profile（Claude Code/Codex/Gemini）：无需 API 密钥，组内固定状态行只读检测「已连接」；点「连接」在内嵌终端执行 CLI 登录命令；启动不注入 API env 且清除同协议残留密钥变量；检测到 CLI 配置文件中残留的 API 密钥（会覆盖账号登录）时显示黄色「N 项配置冲突」
- 供应商预设补齐：claude-code 新增 DeepSeek、智谱 GLM（Anthropic 兼容端点）；codex/qwen/kimi/opencode 补 DeepSeek/智谱 GLM 等
- brew 渠道版本提示：brew 已最新但上游 npm 有更高版本时，组头显示「brew 渠道最新；上游 npm 已到 X（渠道通常滞后）」
- 终端字体一键安装：Maple Mono NF CN / Sarasa Mono SC / Iosevka 未安装时下拉旁出现「安装」，经 Homebrew 装完即生效
- 内置 AI 按功能独立配 profile：提交信息/摘要/PR 描述/冲突建议/技能提炼/翻译六个功能行各自选择 profile，不再共用一个 AI 专用配置

**界面**

- 白话双层呈现：主文案一律白话（保存到历史 / 多出 N 个保存点 / 改动说明 / 相对主分支），git 术语降为悬浮提示与小字 mono，不加模式开关
- 工作区页精简：行内只留任务名、状态与主路径（⌨ 终端 / 评审），PR 与归档收口到统一评审覆盖层内确认；进行中的冲突保留直接「解决冲突」入口
- 终端左栏新增「项目区」：固定列出所有建有活跃工作区的项目（每仓一小节：组头 + 主文件夹节点 + 活跃工作区行），当前 cwd 命中项目置顶标注「当前」、无活跃工作区也保留；点击树与终端 cwd 一起切换，▸ 只读展开一层目录
- 对话页步骤化：工作区会话 ⎇ badge 优先显示流水线步骤名，选中项目节点时列表按步骤分组，搜索覆盖步骤名
- 文件 diff 紧凑配色：增删行整行铺语义深底（bg-ok/bg-err），hunk 标题 inset 底
- 改动面板主从分栏：点文件行 = 左栏 diff 主区 + 右栏紧凑文件列（右栏保持勾选框与状态徽标）
- 终端左栏两段化：常驻 = 项目区 + 文件树；「打开的标签」折叠区删除；「最近项目」收进文件树搜索行 ⌄ 浮层（真进入/↗ 新标签语义不变，最多 4 个且排除当前项目）
- 对话页分类筛选改单列纵向手风琴：点 agent 只展开/收起项目子列表（左侧缩进线表达层级），「全部项目」/单项目行落筛选且面板保持展开（边筛边浏览），选中具体会话才收起
- 侧栏按工作流分层：工作（工作区/终端/对话）→ 能力（配置/技能/统计）→ 底部只留设置，首启默认进工作区；收展完全手动（品牌区点击 + localStorage 记忆）
- 专注双模式：中带「⤢ 专注终端」藏左右栏（标签条留中带顶部），右栏「⇱ 专注内容」铺满、中带只压暗不模糊；Esc 退出，无遮罩
- 键盘流：⌘K 命令面板、⌘1–⌘7 页切（顺序同侧栏）、⌘\ 执行态隐藏侧栏；设置页可自定义快捷键（录制组合键 / 留空禁用 / 恢复默认）
- 工作区项目导航行右键菜单：重命名 / 复制路径 / 移除注册
- 启动栏模型改 combo-box：可输可选（profile 预设 + 每 agent 历史去重、上限 10 条），启动成功即记历史；「新增模型」不再是配置概念
- 界面线条语言：内联内容容器去 1px 描边（底色差 + 圆角 + hairline 分层，边框只给浮层与控件），全站线宽 0.5px，hairline 与底色对比度各主题校准同档；侧栏只留全高竖分界 + 底部管理区一根细线，区间分隔优先留白
- 主题扩至 14 套：七套深色各配一套同性格浅色（设置页浅色对齐排在对应深色正下方），strip/inset/raised 三级浮起梯度各套校准可分辨；深色文本阅读对比度上调；默认主题去蓝校准；步进器完成色 `--color-done` 随主题

**修复**

- macOS 原生 JS 确认/提示框不可用：wry 的 WKWebView 未实现对应委托，`window.confirm` 恒返回 `false`、`window.alert` 静默无效，导致删除、归档、丢弃改动等操作看似无响应。全应用改用 `ConfirmDialog.tsx` 的应用内确认/提示框；同时补齐 Tauri `core:window:allow-close` 与 `core:window:allow-destroy` 权限，修复进入终端页后 Agent 标签可关闭但整个应用无法退出的问题
- Monaco 预览关闭 unicode 高亮：中文全角标点（）：；，与 −× 等符号不再被黄色方框套住（WKWebView locale 不命中导致防 Trojan Source 特性误伤中文笔记）
- 空仓库（git init 后无任何提交）创建工作区自动补空初始提交，不再因无基准分支导致 worktree add 失败；未配置 git 身份时使用临时身份、已配置则沿用
- 统计长会话不再整份跳过：普通 JSONL 与 Codex 压缩会话改逐行流式解析，超过 10MB 的活跃会话也计入；今日/近 7 天/近 30 天按本机时区计算（usage schema 升 v3 自动重建）
- ⇗ 外部终端恢复对 kimi 等官方安装器 CLI 失败（Ghostty 报 failed to launch）：外部拉起的命令改用二进制绝对路径 + shell 改交互登录模式（`-l -i`）——非交互 `zsh -l -c` 不加载 `.zshrc`，`~/.kimi-code/bin` 这类 PATH 会丢
- codex 更新失败/无效：同机多份 node/npm 时，二进制候选目录改为用户目录优先（与应用外终端的解析一致），npm 更新改用与目标二进制同目录的 npm（`updater::npm_for`），不再装错 prefix
- opencode 更新卡在交互选择框：brew 安装的 opencode 改走 `brew upgrade`（自更新 TUI 行输入无法应答）
- kimi-k3 定价修正：内置价从 $0.6/$3.0 改为官方价 ¥20/¥100（输入缓存未命中/输出，每 1M，折 $2.78/$13.89）；缓存命中一折口径不变
- kimi 全局配置补齐 `max_context_size`（0.31+ 必填；k3=1M / k2.6-2.7=256K / 其他=128K 保守默认；旧版 kimi-cli 不写防解析报错）
- ⇗ Ghostty 恢复不再每点一次多开一个程序坞实例：已运行时改走 AppleScript ⌘N 新窗 + 剪贴板粘贴命令（用后还原剪贴板；首次需一次自动化授权）
- 会话删除保护补齐 OpenCode 数据根（`~/.local/share/opencode`），OpenCode 会话删除不再被误拒
- 会话扫描窗口化：普通文件 seek 读头/尾窗，zstd 压缩会话流式解码只留头尾窗（几十 MB 大会话不再整个解压进内存）
- delete_project_sessions 改单连接 + 事务批量删除；全量扫描移入 spawn_blocking（不占 async worker）
- profiles.json / keys.json 读-改-写加序列化锁（并发保存防丢失更新）；pricing.json 写入加数值校验（非负价格、正汇率、逐项报错）
- 会话页「改动」页签对进行中会话加警告条（agent 可能正在写文件，提交前确认）
- Windows 终端页持续闪烁：WebGL 加载前加探针，检测到 SwiftShader 等软件渲染（GPU 被 WebView2 拉黑时）直接回退默认 canvas 渲染——此前仅 try/catch 创建失败，软件渲染「创建成功但一直闪」拦不住
- Windows 修复：关闭 Claude Code 精确注意力开关后 hooks 条目残留（命令内路径反斜杠未归一化导致匹配不上）；Windows 恢复命令行 flag 不再多余加引号；CI Windows 测试口径修正（CRLF 断言归一化、测试文件名避开 Windows 禁用的冒号、手写 YAML 反斜杠转义）

**体验**

- ⇗ 外部恢复 / ⧉ 复制命令的入口提示「使用全局 CLI 配置，不携带本 profile 密钥」（免误解）
- 会话搜索覆盖 AI 摘要内容（项目/标题/标签之外可按摘要关键词找会话）
- 工作区仓库节点直接显示「主仓有改动」黄点（建工作区的决策前置可见）
- 配置页 ▸用量对跨配置共享的模型加 ⚠ 可见标记（用量近似归属会重复计入的提示不用悬停也能看到）
- 技能行显示各 agent 的分发形态（·s=symlink / ·c=copy，悬停含 copy 漂移检测说明）
- ◈ AI 生成（提交信息/摘要/PR 描述/冲突建议）失败时附排查建议（检查 AI 专用配置或换快模型）
- 批量删除二次确认用警示色（bg-err 仅用于此类场景）+「含 pin 快照」明确文案；会话筛选无结果时给「清除筛选」按钮
- 评审面板冲突文件加红色左边条（长列表快速扫视）；文件树 git 状态字母悬停显示中文完整状态
- 侧栏「终端」图标加运行中 agent 数徽标（任意页面可见）；统计页 agent 条右侧加「N 模型」（按使用过的不同模型计）
- 终端调色板抽出共享表（`src/terminal-palettes.ts`），设置页色卡预览升级为真实 8 个 ANSI 色（与终端生效色同源）
- 诊断日志加「导出」按钮（txt 落 ~/Downloads/ccode-exports/，配合反馈问题流程）

**模块**

- 配置中心：六 agent（Claude Code/Codex/Gemini/Qwen/OpenCode/Kimi）多配置管理、多模型切换（编辑表单提示各 agent 切换页上限）、CLI 安装/更新（组头版本号 + 新版/已更新/更新三态）、连接测试
- 工作区：git worktree 任务隔离、`.ccode/settings.toml` 自动化（files-to-copy/setup/archive 脚本/端口段）、评审面板（任务 diff + 逐文件彩色 diff + 冲突闭环：并入主分支、两侧内容预览、逐文件/一键选边、◈ AI 选侧建议）、合并拆分「合并（保留工作区，显示已合并 pill）/ 合并并归档」、主仓库状态前置健康检查、归档后滞留终端自动回主仓库
- 终端：多标签 xterm.js、agent 退出回落 shell、会话一键恢复、Monaco 文件预览编辑（稳定文档模型、外部改写自动刷新、主仓库保存二次确认、路径归属标识 + 主项目⇄多分支切换）、真实 cwd 跟踪（文件树/git 面板跟随 shell 内 cd）、git 改动面板（成功 toast）、⌘F 输出搜索、专注模式（只剩终端，标签与动作菜单移到侧栏）、◈ AI 提交信息
- 会话：六 CLI 会话解析回放（含外部运行会话感知）、pin 快照/标签/归档/搜索/批量删除、恢复三入口（内嵌终端 / ⇗ 外部终端 / ⧉ 复制恢复命令）、◈ AI 摘要、Markdown 导出
- 技能：Skills 统一库 + 六 CLI 分发、目录/ZIP/GitHub 导入、ZIP 导出、分类管理
- 统计：token/费用统计（官方价、$/¥ 切换）、agent 占比进度条
- 设置：七套深色主题、终端字体/调色板、AI 专用配置、外部终端首选、自动更新、诊断日志

**性能**

- 前端分包：首屏 4.7MB → 224KB；终端页 memo 化与轮询门控；输出合帧
- list_repos 60s 缓存

**修复与安全**

- 工作区起点从 origin 主分支改为本地主分支（未推送提交不再丢失）
- 删除保护 canonicalize 双校验（堵符号链接绕过）；codex 默认沙箱（交互 workspace-write / 无头 read-only）
- CLI 二进制解析 GUI 短 PATH 兜底（含 `~/.kimi-code/bin` 候选，修复打包版 kimi 检测）
- 预览编辑器稳定文档模型（同名文件跨仓库静默切换曾导致误改主仓库，该「跟随」机制已移除并禁止复活）
- 密钥只在拉起瞬间注入；外部恢复/复制恢复命令不携带 profile 密钥
- macOS 暂未签名公证：首次打开需 `xattr -cr /Applications/Ccode.app`
