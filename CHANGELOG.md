# 更新日志

本项目按[语义化版本](https://semver.org/lang/zh-CN/)管理。架构级决策记录见 [docs/architecture.md](docs/architecture.md) §10。

## v0.1.0（首个发布版本）

**科研流水线**

- 项目注册与 `.ccode/project.toml` 档案卡：工作区页按项目分组，组顶部为流水线大圆步进器概览（方块节律虚线贯穿 + 纯色状态大圆 + 圆前/圆后小方块（混在虚线节律里、hover 才现可点）= 编辑简报/产物入口 + ⋯），点大圆按状态推进步骤（开步/恢复/跳终端），状态从绑定工作区自动派生，标题行显示「研究流程 d/t」计数与「⚠ N 条提示」校验徽标；未注册目录组头「注册项目」与顶部「+ 添加项目」走同一注册弹窗
- 一键开步：点未开始步骤的大圆 = 非 git 目录引导 init（自动生成含 `*.pdf` 的 .gitignore）→ 自动把 `.ccode` 与 `.gitignore` 提交进主仓库（仅这两个自有路径）→ 建绑定工作区 → 简报落成 `TASK.md`（自动加入 `.git/info/exclude`，不随合并污染主项目）→ 终端预填启动；课题主题可编辑并写进 TASK.md
- 流水线模板库：内置四套（英文综述/科研论文/数据处理/毕业论文）+ 当前流水线「另存为模板」（用户模板可删除）；「重置为模板」替换步骤不影响已绑定工作区
- 「编辑流水线」全宽编辑器是步骤编辑唯一入口：每步一张卡片（步骤名/工作区名/简报/预期产物/run 脚本/资源绑定/推荐技能），↑↓ 排序、整体写回；资源绑定默认空 = 全部资源，勾选后 TASK.md「项目资源」段只列选中项
- 资源面板：登记 PDF/CSV/parquet/bib 等资料（只记路径不复制），「发现资源」自动扫描项目目录列候选一键登记，资源行可复制绝对路径粘贴给 agent；PDF 资源直接内嵌「查看」
- 步骤产物核验面板（任务行「产物」与步进器圆后小方块两处同一面板）：进行中/待评审列工作树下的预期产物，已完成后列主文件夹同名产物，点击文件跳终端页预览；步骤「⋯」内「◫ 定位目录」把终端文件树根切到该工作区
- 提货单 `artifacts.yaml`：改动面板登记大产物（md5 + 大小，已 git 跟踪的文件拒绝登记），清单随分支提交；下一步开步时项目根提货单自动写进 TASK.md「上一步产物」段
- 论文类模板自带 quarto 脚本：开步写入 `.ccode/settings.toml`，工作区行「运行脚本」出现 `render-draft` / `render-final` 渲染 PDF 与 `export-docx` 导出 docx
- 评审合并成功「▶ 开始下一步」：合并成功且保留工作区时，成功横幅给出「▶ 开始下一步：步骤名」（下一步 = 同名步骤之后第一个无同名工作区、含已归档的步骤）；无下一步只显示合并成功横幅，「合并并归档」不出此入口；开步 invoke 链路单一出处 `pipeline-start.ts`，步进器大圆与评审入口共用

**阅读与写作**

- PDF 内嵌预览（pdf.js，懒加载分包）：页码导航/跳页/缩放，只渲染当前页与相邻页；拖选文字浮出「◈ 问 AI」把选段 + 出处（文件名、页码）写入活跃终端输入框（不自动发送）；浮动条另有「↵ 直接发送」——选段 + 出处一次拼接回车注入，同帧到达防半截输入
- 「整理为笔记」：PDF 选段（含出处）追加到归属项目「文献精读与笔记」工作区的 `notes/inbox.md`；没有该工作区时自动一键开步创建并预填终端启动；未登记资源的 PDF 提示去工作区页登记
- Markdown 阅读版式：md 预览默认渲染排版（标题/引用/表格/代码块，配色随主题），「阅读/编辑」切换不丢未保存改动；「⛶ 沉浸阅读/沉浸编辑」全宽覆盖层（Esc 退出，终端保持运行）
- 笔记对话式批改：md 阅读版式（含沉浸）拖选文字浮出「◈ 讨论/改写此段」，选段 + 出处写入活跃终端 agent 输入框，接着输入你的意见后自行发送
- docx 阅读版式预览：mammoth 转排版视图（图片内嵌，配色随主题），只读；超过 50MB 明确提示不渲染
- 「◈ 接力到…」：从当前会话尾部生成结构化接力简报（脱敏后落 `.ccode/handoff-<时间>.md`），新终端标签带简报启动目标 Agent；Kimi/OpenCode 无注入参数走复制简报路径手动发送；接力会话在对话页显示「⇄ 接自 <Agent>」，接力链可回溯

**评审与验收**

- 逐 hunk 验收：改动面板展开的单文件 diff 中每个改动块可单独「丢弃」（`git apply -R`，二次确认）或「暂存」（`git apply --cached`）；勾选提交遇部分暂存走临时索引提交，只带走已暂存的块；新文件整个算一块（暂存=跟踪，丢弃=删文件）；仅覆盖未提交改动，已提交累计 diff 仍走评审
- 评审内快速提交：合并被「主文件夹里还有没保存的改动」拦截时，可在评审覆盖层内直接列出主仓文件勾选提交（默认不勾选、信息留空走本地默认、不调 AI），成功自动重新健康检查
- 图片双栏看图：评审与改动面板中 png/jpg/gif/webp/svg/bmp 直接「基准 vs 当前」对照（透明图带棋盘格底），单图超 20MB 才回落提示

**监督**

- 长任务 OS 通知：agent 从工作中转为待确认/已完成且窗口未聚焦时发系统通知（同标签 30 秒去抖，设置页可关）；通知带「去处理」动作——已完成且 cwd 命中工作区直达评审覆盖层，待确认聚焦对应终端标签
- 首页「待你处理」收件箱：聚合工作区冲突/可合并 + 终端待确认/已完成，排序 冲突 > 待确认 > 可合并 > 已完成，为空整块不渲染；横跨项目导航与详情两栏之上，跳终端激活标签走一次性请求（已关闭标签静默忽略）；终端运行状态经镜像跨页只读（不新增轮询），外部 live 会话由后端 `session_tail_state` 直查；原左栏「运行中」折叠面板移除
- 端口监控：LISTEN 端口列表区分「本应用 / 系统其他」两段（cwd 最长前缀归属，回落 `CCODE_PORT` 段标注），终止系统其他进程必须二次确认
- 任务成本：统计页按工作区/任务归因 tokens 与费用（usage schema 升 v5 自动重建索引）
- 订阅口径：官方账号（如 Google 登录）启动产生的用量显示「订阅」不按量计费，token 数仍实际统计（schema 升 v6）
- 对话页新增「历史时间线视图」

**技能**

- 新建/编辑技能：「+ 新建技能」与行内「⋯ → 编辑内容」内联编辑名称/描述/正文（frontmatter 自动生成），重名拒绝并引导改用编辑，保存前自动备份（保留最近 5 份）
- 「◈ 优化」：写下优化意见后开终端标签，Agent 阅读并直接改写库中的 SKILL.md，备份兜底靠既有保存/覆盖路径
- 选段「✦ 沉淀为技能」：md/PDF 选段 → AI 提炼 SKILL.md 草稿（脱敏 + 8KB 截断 + JSON 容错解析）→ 技能页新建弹窗预填，保存走既有新建路径（重名拒绝）
- 技能列表增强：用户自定义标签（名称后 1–4 个 pill）、GitHub 导入自动分类、来源 owner/repo 显示为可点链接、详情「◈ 翻译为中文」
- 步骤推荐技能：步骤可挂 `skills = [...]`，一键开步的 TASK.md 落成「本步骤推荐技能」段（未安装的标注）
- 科研技能包：`lit-search` / `lit-notes` / `review-framework` / `review-writing`（综述）、`data-clean` / `data-eda`（数据）、`quarto-render`（渲染排错）

**配置与账号**

- 官方账号 profile（Claude Code/Codex/Gemini）：无需 API 密钥，组内固定状态行只读检测「已连接」；点「连接」在内嵌终端执行 CLI 登录命令；启动不注入 API env 且清除同协议残留密钥变量；检测到 CLI 配置文件中残留的 API 密钥（会覆盖账号登录）时显示黄色「N 项配置冲突」
- 供应商预设补齐：claude-code 新增 DeepSeek、智谱 GLM（Anthropic 兼容端点）；codex/qwen/kimi/opencode 补 DeepSeek/智谱 GLM 等
- brew 渠道版本提示：brew 已最新但上游 npm 有更高版本时，组头显示「brew 渠道最新；上游 npm 已到 X（渠道通常滞后）」
- 终端字体一键安装：Maple Mono NF CN / Sarasa Mono SC / Iosevka 未安装时下拉旁出现「安装」，经 Homebrew 装完即生效
- 内置 AI 按功能独立配 profile：提交信息/摘要/PR 描述/冲突建议/技能提炼/翻译六个功能行各自选择 profile，不再共用一个 AI 专用配置

**界面**

- 白话双层呈现：主文案一律白话（保存到历史 / 多出 N 个保存点 / 改动说明 / 相对主分支），git 术语降为悬浮提示与小字 mono，不加模式开关
- 工作区页精简：行内只留任务名、状态与主路径（⌨ 终端 / 评审），PR 与归档收口到统一评审覆盖层内确认；进行中的冲突保留直接「解决冲突」入口
- 终端左栏新增「项目区」：固定列出所有建有活跃工作区的项目（每仓一小节：组头 + 主文件夹节点 + 活跃工作区行），当前 cwd 命中项目置顶标注「当前」、无活跃工作区也保留；点击树与终端 cwd 一起切换，▸ 只读展开一层目录
- 对话页步骤化：工作区会话 ⎇ badge 优先显示流水线步骤名，选中项目节点时列表按步骤分组，搜索覆盖步骤名
- 文件 diff 紧凑配色：增删行整行铺语义深底（bg-ok/bg-err），hunk 标题 inset 底
- 改动面板主从分栏：点文件行 = 左栏 diff 主区 + 右栏紧凑文件列（右栏保持勾选框与状态徽标）
- 终端左栏两段化：常驻 = 项目区 + 文件树；「打开的标签」折叠区删除；「最近项目」收进文件树搜索行 ⌄ 浮层（真进入/↗ 新标签语义不变，最多 4 个且排除当前项目）
- 对话页分类筛选改单列纵向手风琴：点 agent 只展开/收起项目子列表（左侧缩进线表达层级），「全部项目」/单项目行落筛选且面板保持展开（边筛边浏览），选中具体会话才收起
- 侧栏按工作流分层：工作（工作区/终端/对话）→ 能力（配置/技能/统计）→ 底部只留设置，首启默认进工作区；收展完全手动（品牌区点击 + localStorage 记忆）
- 专注双模式：中带「⤢ 专注终端」藏左右栏（标签条留中带顶部），右栏「⇱ 专注内容」铺满、中带只压暗不模糊；Esc 退出，无遮罩
- 键盘流：⌘K 命令面板、⌘1–⌘7 页切（顺序同侧栏）、⌘\ 执行态隐藏侧栏；设置页可自定义快捷键（录制组合键 / 留空禁用 / 恢复默认）
- 工作区项目导航行右键菜单：重命名 / 复制路径 / 移除注册
- 启动栏模型改 combo-box：可输可选（profile 预设 + 每 agent 历史去重、上限 10 条），启动成功即记历史；「新增模型」不再是配置概念
- 界面线条语言：内联内容容器去 1px 描边（底色差 + 圆角 + hairline 分层，边框只给浮层与控件），全站线宽 0.5px，hairline 与底色对比度各主题校准同档；侧栏只留全高竖分界 + 底部管理区一根细线，区间分隔优先留白
- 主题扩至 14 套：七套深色各配一套同性格浅色（设置页浅色对齐排在对应深色正下方），strip/inset/raised 三级浮起梯度各套校准可分辨；深色文本阅读对比度上调；默认主题去蓝校准；步进器完成色 `--color-done` 随主题

**修复**

- Monaco 预览关闭 unicode 高亮：中文全角标点（）：；，与 −× 等符号不再被黄色方框套住（WKWebView locale 不命中导致防 Trojan Source 特性误伤中文笔记）
- 空仓库（git init 后无任何提交）创建工作区自动补空初始提交，不再因无基准分支导致 worktree add 失败；未配置 git 身份时使用临时身份、已配置则沿用
- 统计长会话不再整份跳过：普通 JSONL 与 Codex 压缩会话改逐行流式解析，超过 10MB 的活跃会话也计入；今日/近 7 天/近 30 天按本机时区计算（usage schema 升 v3 自动重建）
- ⇗ 外部终端恢复对 kimi 等官方安装器 CLI 失败（Ghostty 报 failed to launch）：外部拉起的命令改用二进制绝对路径 + shell 改交互登录模式（`-l -i`）——非交互 `zsh -l -c` 不加载 `.zshrc`，`~/.kimi-code/bin` 这类 PATH 会丢
- codex 更新失败/无效：同机多份 node/npm 时，二进制候选目录改为用户目录优先（与应用外终端的解析一致），npm 更新改用与目标二进制同目录的 npm（`updater::npm_for`），不再装错 prefix
- opencode 更新卡在交互选择框：brew 安装的 opencode 改走 `brew upgrade`（自更新 TUI 行输入无法应答）
- kimi-k3 定价修正：内置价从 $0.6/$3.0 改为官方价 ¥20/¥100（输入缓存未命中/输出，每 1M，折 $2.78/$13.89）；缓存命中一折口径不变
- kimi 全局配置补齐 `max_context_size`（0.31+ 必填；k3=1M / k2.6-2.7=256K / 其他=128K 保守默认；旧版 kimi-cli 不写防解析报错）
- ⇗ Ghostty 恢复不再每点一次多开一个程序坞实例：已运行时改走 AppleScript ⌘N 新窗 + 剪贴板粘贴命令（用后还原剪贴板；首次需一次自动化授权）
- 会话删除保护补齐 OpenCode 数据根（`~/.local/share/opencode`），OpenCode 会话删除不再被误拒
- 会话扫描窗口化：普通文件 seek 读头/尾窗，zstd 压缩会话流式解码只留头尾窗（几十 MB 大会话不再整个解压进内存）
- delete_project_sessions 改单连接 + 事务批量删除；全量扫描移入 spawn_blocking（不占 async worker）
- profiles.json / keys.json 读-改-写加序列化锁（并发保存防丢失更新）；pricing.json 写入加数值校验（非负价格、正汇率、逐项报错）
- 会话页「改动」页签对进行中会话加警告条（agent 可能正在写文件，提交前确认）
- Windows 终端页持续闪烁：WebGL 加载前加探针，检测到 SwiftShader 等软件渲染（GPU 被 WebView2 拉黑时）直接回退默认 canvas 渲染——此前仅 try/catch 创建失败，软件渲染「创建成功但一直闪」拦不住

**体验**

- ⇗ 外部恢复 / ⧉ 复制命令的入口提示「使用全局 CLI 配置，不携带本 profile 密钥」（免误解）
- 会话搜索覆盖 AI 摘要内容（项目/标题/标签之外可按摘要关键词找会话）
- 工作区仓库节点直接显示「主仓有改动」黄点（建工作区的决策前置可见）
- 配置页 ▸用量对跨配置共享的模型加 ⚠ 可见标记（用量近似归属会重复计入的提示不用悬停也能看到）
- 技能行显示各 agent 的分发形态（·s=symlink / ·c=copy，悬停含 copy 漂移检测说明）
- ◈ AI 生成（提交信息/摘要/PR 描述/冲突建议）失败时附排查建议（检查 AI 专用配置或换快模型）
- 批量删除二次确认用警示色（bg-err 仅用于此类场景）+「含 pin 快照」明确文案；会话筛选无结果时给「清除筛选」按钮
- 评审面板冲突文件加红色左边条（长列表快速扫视）；文件树 git 状态字母悬停显示中文完整状态
- 侧栏「终端」图标加运行中 agent 数徽标（任意页面可见）；统计页 agent 条右侧加「N 模型」（按使用过的不同模型计）
- 终端调色板抽出共享表（`src/terminal-palettes.ts`），设置页色卡预览升级为真实 8 个 ANSI 色（与终端生效色同源）
- 诊断日志加「导出」按钮（txt 落 ~/Downloads/ccode-exports/，配合反馈问题流程）

**模块**

- 配置中心：六 agent（Claude Code/Codex/Gemini/Qwen/OpenCode/Kimi）多配置管理、多模型切换（编辑表单提示各 agent 切换页上限）、CLI 安装/更新（组头版本号 + 新版/已更新/更新三态）、连接测试
- 工作区：git worktree 任务隔离、`.ccode/settings.toml` 自动化（files-to-copy/setup/archive 脚本/端口段）、评审面板（任务 diff + 逐文件彩色 diff + 冲突闭环：并入主分支、两侧内容预览、逐文件/一键选边、◈ AI 选侧建议）、合并拆分「合并（保留工作区，显示已合并 pill）/ 合并并归档」、主仓库状态前置健康检查、归档后滞留终端自动回主仓库
- 终端：多标签 xterm.js、agent 退出回落 shell、会话一键恢复、Monaco 文件预览编辑（稳定文档模型、外部改写自动刷新、主仓库保存二次确认、路径归属标识 + 主项目⇄多分支切换）、真实 cwd 跟踪（文件树/git 面板跟随 shell 内 cd）、git 改动面板（成功 toast）、⌘F 输出搜索、专注模式（只剩终端，标签与动作菜单移到侧栏）、◈ AI 提交信息
- 会话：六 CLI 会话解析回放（含外部运行会话感知）、pin 快照/标签/归档/搜索/批量删除、恢复三入口（内嵌终端 / ⇗ 外部终端 / ⧉ 复制恢复命令）、◈ AI 摘要、Markdown 导出
- 技能：Skills 统一库 + 六 CLI 分发、目录/ZIP/GitHub 导入、ZIP 导出、分类管理
- 统计：token/费用统计（官方价、$/¥ 切换）、agent 占比进度条
- 设置：七套深色主题、终端字体/调色板、AI 专用配置、外部终端首选、自动更新、诊断日志

**性能**

- 前端分包：首屏 4.7MB → 224KB；终端页 memo 化与轮询门控；输出合帧
- list_repos 60s 缓存

**修复与安全**

- 工作区起点从 origin 主分支改为本地主分支（未推送提交不再丢失）
- 删除保护 canonicalize 双校验（堵符号链接绕过）；codex 默认沙箱（交互 workspace-write / 无头 read-only）
- CLI 二进制解析 GUI 短 PATH 兜底（含 `~/.kimi-code/bin` 候选，修复打包版 kimi 检测）
- 预览编辑器稳定文档模型（同名文件跨仓库静默切换曾导致误改主仓库，该「跟随」机制已移除并禁止复活）
- 密钥只在拉起瞬间注入；外部恢复/复制恢复命令不携带 profile 密钥
- macOS 暂未签名公证：首次打开需 `xattr -cr /Applications/Ccode.app`
