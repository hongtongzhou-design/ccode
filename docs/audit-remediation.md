# 系统审计收口：已落地、未验收、未做

日期：2026-09-08。这不是「全部完成」清单。架构 §10 与 `AGENTS.md` 引用本文件，用来把**已经改的代码**、**还没实机验收的部分**和**明确不做的范围**分开，避免再次把入口未覆盖或未验证说成已收口。

## 1. 已经落地、不要再列为「未做」

- 文档 HTML 清洗与生产 CSP；Vite 显式 `vite.config.ts`
- 文件预览版本检查、截断保护、保存期间 dirty 保护
- 定时任务冻结产物评审与安全采纳（`watch_review.rs`）
- 普通科研/办公目标采纳：采纳前备份、写入前再读基线、失败回滚（`runs.rs` `task_adopt_outputs`）
- 任务书草稿保存必须带读取时的 revision；冲突拒绝覆盖（`write_task_draft`）
- 配置创建/修改：网关与绑定成对写入，第二份失败回退第一份；密钥写入失败回退清单；`delete_key` 失败上报，不假装已清除
- Run 结束：账本事务失败仍释放存活锁并记诊断，避免「进程已退出、账本仍运行」
- 后台命令总超时、有界输出、取消入口
- PTY 独立输入队列、异步诊断输出记录
- 配置跨进程锁、私有临时文件权限、迁移 pending 恢复日志
- 普通日志出站脱敏
- 已报 npm 漏洞的定向升级

## 2. 代码已改、但不能算验收完成

这些不是「没写」，是缺最后的验证。

| 项 | 缺口 |
|---|---|
| Windows 完整构建与运行 | 本机可交叉类型检查；完整 Windows 应用构建受 C SDK/`ring`/`assert.h` 环境限制。Job 回收、ConPTY 停止、只读文件替换、OS 文件锁需 Windows 实机。 |
| macOS / Linux 桌面验收 | 未在指定 Mesa Dev 窗口做完整点选：CSP 对 PDF/Monaco/图片/公式/热更新、关窗确认框、停止任务后的界面/通知、编辑冲突与冻结产物评审的真实交互。 |
| 九个 CLI 新建与恢复权限 | 代码注入只读/计划模式，缺能力拒绝。未逐家实机跑参数组合。CLI 计划模式 ≠ 操作系统只读沙箱。 |
| 真实双实例与异常退出 | 锁有单元测试。缺：两实例同时改配置/跑定时、强制结束再恢复、新旧版本并行、写盘中断电或磁盘满。 |
| 发布交付 | 未提交、未打安装包、未发布。工作树仍混有其他任务修改，发布前必须冻结验收基线。 |

## 3. 明确未做、且不作为本次审计必须项

不要为了「收口」去做这些：全面拆大文件、合并两套 worktree、换框架、统一改写存储层。

尚未落地的优化（不阻塞审计修复本身）：

- 其他数据库入口仍反复 schema 检查；事件未形成完整批量事务
- 可见终端输出背压不是严格容量上限；缺压力测试
- 诊断丢弃没有计数与明确提示
- 冻结证据/备份没有保留期限与安全清理入口
- 没有冷启动/千级会话/多高输出终端/大 PDF 的可复现基准
- 没有「CLI 版本 × 平台 × 启动/恢复/取消/权限」持续集成矩阵
- 冲突拒绝覆盖后，没有一键对比/另存/三方合并

## 4. 本轮补上的入口（相对「定时/预览已修、其它入口未修」）

- **普通目标采纳**不再逐文件 `fs::copy` 后无法回滚：先锁、备份原件、写入前核对基线，后一文件失败则回滚已写文件。主仓在采纳期间被改过的文件 fail-closed。
- **任务书弹层**与决策落盘、文献来源同步、播种 TASK.md 均携带 `expectedRevision`。外部改稿冲突时不覆盖；保存期间继续输入保持 dirty。
- **连接创建/修改**在密钥、网关、绑定之间失败时回退已写清单，并让密钥清除失败回到调用方。
- **Run 关闭**在数据库失败时仍释放 lease，并写诊断；不把忽略错误当成登记成功。

以上仍是乐观并发 + 成对回退，不是跨文件崩溃原子事务，也不锁住不走 Mesa 的外部编辑器。
