---
name: figure-forge
description: 投稿级图表生产规范。当用户要求把分析结果画成论文/投稿用图、按期刊规格调整图（尺寸/分辨率/字体）、或把探索性草稿图升级为投稿质量时使用。负责「怎么画到位」；图的内容设计与选型见 figure-designer 技能。全部图由可复现脚本生成，禁止手工改图。
---

# 投稿级图表生产（figure-forge）

本技能规定「数据 → 可复现脚本 → 投稿规格图表」的生产规范。与 figure-designer 的分工：figure-designer 决定画什么、怎么布局；本技能负责把图画到期刊能收的规格。

## 何时使用

- 用户要求把 results/analysis 的数字画成论文用图，或把 EDA 草稿图升级为投稿质量
- 用户给出目标期刊/会议的图表要求，要求逐条适配
- 科研论文/毕业论文流水线中「结果分析」「实验与结果」步骤的出图环节

## 操作规范

### 1. 数字出处先行

- 画图用的数据必须来自项目内的结果文件（`results/summary.md`、`analysis/` 或产物目录），脚本从文件读数，**不把数字手抄进代码**，更不得为美观调整数据；
- 数据文件缺失时停止并说明，不虚构示例数据冒充结果。

### 2. 期刊规格适配

目标期刊有图表要求（作者指南）时逐条核对；无明确要求时按通用投稿规格：

- **物理尺寸**：单栏图宽约 8-9 cm、双栏约 17-18 cm，按目标版式设 figsize，不靠缩放凑；
- **分辨率**：位图元素 ≥ 300 dpi（照片/热图类 600 dpi 更稳）；
- **字体字号**：字体嵌入（PDF/SVG 转曲或嵌入），成稿字号 7-9 pt，全图统一字族；
- **线宽与标记**：坐标轴/数据线 ≥ 0.5 pt，标记点在成稿尺寸下可分辨；
- **格式**：线条图矢量（PDF/SVG/EPS）优先；含连续色调的位图部分存 PNG/TIFF。

### 3. 可读性底线

- 配色色盲友好（避免红绿对比，可用 Okabe-Ito 调色板）；黑白打印下仍可区分（辅以线型/标记差异）；
- 坐标轴标物理量与单位；图例不遮数据；显著性标记的含义在图注说明；
- 投稿场景标注用英文；数值有效数字与正文表格一致。

### 4. 脚本与文件组织

- 出图脚本放 `figures/`（或 `analysis/figures/`），一图一函数，设 `main()` 入口，从 `main` 重跑产出相同图片；
- 路径集中为模块顶部常量；风格参数（字号/配色/线宽）集中定义，全部图共享；
- 输出文件命名与正文引用一一对应（如 `fig2-ablation.pdf`）；中间稿不堆积，旧版直接覆盖。

### 5. 交付前复查清单

逐图过一遍：成稿物理尺寸下文字可读 / 色盲友好 / 坐标轴单位齐全 / 数字与正文一致 / 图注自含（不依赖正文也能看懂图在比什么）。复查结论逐图一行，写进 `figures/README.md` 或出图脚本的输出。

## 完成标准

每张图可由脚本一键重出；规格符合目标期刊要求（或通用规格）；复查清单逐项有结论；数据未经任何手工改动。

- 写作约定：不使用彩色 emoji；强调标记用「注意：」或单色 ⚠（U+26A0 U+FE0E）。

---

理念参考：Yuan1z0825/nature-skills（Apache-2.0）nature-figure、Orchestra-Research/AI-Research-SKILLs（MIT）academic-plotting、wanshuiyin/Auto-claude-code-research-in-sleep（MIT）paper-figure；内容为按 Ccode 科研工作流重写。
